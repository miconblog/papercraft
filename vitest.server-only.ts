/**
 * `server-only` 대역 (IDE-013)
 *
 * 그 패키지는 서버 번들이 아닌 곳에서 불리면 던지는 것이 하는 일의 전부인데,
 * 테스트 환경은 jsdom 이라 클라이언트로 잡힌다 — 그대로 두면 서버 모듈을
 * 하나도 못 불러온다. `exports` 가 막혀 있어 패키지 안의 빈 파일을 직접
 * 가리킬 수 없어서 여기에 둔다.
 *
 * 진짜 방어선은 `next build` 다 — 클라이언트 컴포넌트가 서비스 롤 키를 끌고
 * 오면 빌드가 깨진다. 여기서 비운다고 그 방어가 약해지지 않는다.
 */
export {};
