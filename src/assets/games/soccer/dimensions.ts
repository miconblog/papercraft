/**
 * 축구 게임판 실측 치수 (IDE-004)
 *
 * 여기 있는 값이 **배율 100%에서 종이 위 mm**다. 도안 정의(`./index.ts`)와
 * 아트워크 생성기(`./artwork/`)가 같은 상수를 읽는다 — 한쪽만 고치면 슬롯
 * 좌표와 그림이 어긋나므로 치수는 이 파일 하나에만 둔다.
 *
 * 좌표계는 `docs/game-authoring.md`를 따른다: 파트 로컬 mm, 원점 좌상단,
 * y는 아래로 증가한다.
 */

/** 보드(운동장) 파트. 배율 100%에서 A4를 가로로 놓은 크기다(IDE-002). */
export const BOARD = { widthMm: 297, heightMm: 210 } as const;

/**
 * 터치라인 사각형 = 실제로 공을 튕기는 면.
 *
 * **보드를 거의 다 쓴다.** 사방 6mm만 남기는데, 재단 오차에도 라인이 잘리지
 * 않을 만큼이다. 예전에는 위아래 15mm를 띠로 떼어 팀 이름과 제목을 넣었지만
 * 그 글자들은 운동장에서 뺐다(2026-09-05) — 놀 면을 좁히면서까지 종이에 남길
 * 이유가 없었다. 팀 이름은 점수 기록칸에 남는다.
 *
 * 285×198은 가로세로비 1.439로 실제 축구장 100×69.5m에 해당한다. 국제 규정이
 * 정한 범위(길이 90–120m · 폭 45–90m) 안이면서, 보드(297×210, 비율 1.414)를
 * 가장 꽉 채우는 비율이다.
 */
export const FIELD = {
  xMm: 6,
  yMm: 6,
  widthMm: 285,
  heightMm: 198,
} as const;

export const FIELD_RIGHT_MM = FIELD.xMm + FIELD.widthMm; // 289
export const FIELD_BOTTOM_MM = FIELD.yMm + FIELD.heightMm; // 195
export const FIELD_CENTER_X_MM = FIELD.xMm + FIELD.widthMm / 2; // 148.5
export const FIELD_CENTER_Y_MM = FIELD.yMm + FIELD.heightMm / 2; // 105

/**
 * 종이 골대 — 오려 접어 골라인 앞에 놓는 **뚜껑 달린 쟁반**.
 *
 * 모양은 쓰레받기(사용자 표현으로 "국자")다. 바닥 하나, 벽 셋(뒤·좌·우), 그 위를
 * 덮는 뚜껑, 앞으로 납작하게 뻗어 나온 **입술**. 공은 운동장에서 입술을 타고
 * 굴러 들어와 쟁반 안에 멈추고, 골문(70×24mm)으로 들여다보면 그 공이 보인다.
 *
 * 구조가 다섯 번 바뀌었다. 전부 놀아 보고 나온 문제를 고친 것이다:
 *
 * 1. 처음(2026-09-04)에는 골라인 **안쪽**에 서는 터널이었다. 골 판정 평면이
 *    골라인보다 16mm 안쪽에 생기고, 골 에어리어를 실제의 2배로 늘려야 했다.
 * 2. 깊이를 없애고 골라인 위에 서는 평면 ⊓ 프레임으로 바꿨더니(2026-09-05)
 *    **공이 골문을 순식간에 지나가 버려 골인지 아닌지 보이지 않았다**.
 * 3. 그래서 지붕에 창을 낸 **바닥 없는 상자**로 돌아갔다(2026-09-05). 공은
 *    멈춰 섰지만, 지붕을 붙이려면 풀칠이나 칼집이 필요했다.
 * 4. 뚜껑 없는 쟁반으로 바꿨다(2026-09-06 사용자 요청). 지붕을 없애니 창도,
 *    풀칠탭도, 칼집도 필요 없어졌다.
 * 5. 지금은 그 쟁반에 **뚜껑을 다시 덮었다**(2026-09-08 사용자 지적). 위가
 *    뚫려 있으면 골포스트를 맞고 튄 공이 그대로 밖으로 나가 버린다. 다만 3번의
 *    지붕과는 다르다 — 뚜껑은 뒷벽 위에 **경첩처럼 달려 앞으로 덮이고**, 양옆
 *    귀를 옆벽 바깥에 씌워 닫는다. 붙일 것이 없으니 풀도 칼도 돌아오지 않는다.
 *
 * 3번까지는 바닥이 없었다. 바닥을 깔면 종이 두께만큼 턱이 생겨 미끄러져 오는
 * 공이 골문에서 걸린다는 이유였는데, **입술**이 그 문제를 없앤다: 바닥이 그대로
 * 앞으로 뻗어 나와 운동장 위에 얹히므로 공이 넘을 턱이 아예 없다. 그래서 골대를
 * 운동장 위에 놓든, 스케치북을 오려 그 자리에 끼우든 똑같이 굴러 들어간다.
 *
 * 크기는 **사용자가 아이와 손으로 만들어 쓰던 골대를 사진에서 실측한 값**이다.
 * 실제 축척(2.85mm/m)을 따르지 않는다 — 아이와 하는 종이 게임이라 골이 들어가는
 * 맛이 먼저고, 그러려면 실제 골대보다 커야 한다(사용자 지적).
 */
export const GOAL = {
  /**
   * 골문 폭 = 쟁반 바닥의 폭 = 입술의 폭.
   *
   * 사진(`IMG_3244.JPG`)에서 잰 값이다. 운동장에 인쇄된 골포스트 눈금 두 개가
   * 정확히 39mm 간격이라 그것을 자로 삼아 축척 12.0 px/mm를 얻고, 같은 사진 속
   * 수제 골대의 뒷벽을 재 약 68mm를 얻었다. 반올림해 70으로 둔다.
   *
   * 실제 7.32m를 이 축척으로 줄이면 20.9mm이므로 **약 3.3배 과장**이다. 공이
   * 실제 축척보다 19배 크다는 것을 생각하면 여전히 공에 비해 작은 골대다.
   */
  mouthWidthMm: 70,
  /**
   * 벽 셋의 높이. 앞이 뚫려 있으므로 이 값이 곧 **골문 높이**이고, 뚜껑을 덮으면
   * 뚜껑 앞 모서리가 그 위를 가로지르는 **크로스바**가 된다.
   *
   * 지름 12mm 공의 두 배다. 낮으면 굴러 들어온 공이 뒷벽을 타고 튀어 나간다.
   */
  wallHeightMm: 24,
  /**
   * 바닥 깊이(벽 안쪽). 공 지름의 2.7배라 들어온 공이 넉넉히 멈춘다.
   *
   * 얕으면 뒷벽에 맞은 공이 도로 튀어 나온다.
   */
  trayDepthMm: 32,
  /**
   * 바닥이 앞으로 그대로 뻗어 나온 **입술**의 길이.
   *
   * 쓰레받기의 그 입술이다. 운동장 위에 얹으면 공이 넘을 턱이 없고, 스케치북을
   * 골문 폭만큼 오려 낸 자리에 끼우면 골대가 제자리에 고정된다 — 넣고 빼기가
   * 쉬운 것이 이 모양을 고른 이유다(2026-09-06 사용자 요청).
   */
  lipDepthMm: 20,
  /**
   * 뒷벽 위에 경첩처럼 달려 앞으로 덮이는 **뚜껑**의 깊이 (2026-09-08).
   *
   * 위가 뚫려 있으면 골포스트를 맞고 튄 공이 밖으로 나간다는 사용자 지적으로
   * 넣었다. **바닥 깊이와 같아야** 쟁반 위를 빈틈없이 덮고, 그때 뚜껑 앞
   * 모서리가 골문 바로 위에 와 크로스바가 된다 — 테스트가 이 등식을 지킨다.
   *
   * 공은 이제 위가 아니라 **골문으로 보인다**. 70×24mm 구멍이라 눈높이를 낮추면
   * 안에 멈춘 공이 그대로 보이고, 꺼낼 때는 뚜껑을 젖히면 된다.
   */
  lidDepthMm: 32,
  /**
   * 뚜껑 양옆에서 **바깥으로** 접어 옆벽에 씌우는 귀.
   *
   * 이 도안에서 **유일한 산접기**다. 상자 뚜껑을 씌우듯 벽 바깥을 감싸야 뚜껑이
   * 들리지 않는다 — 안으로 접으면 옆벽의 겹과 같은 자리를 다투고, 그냥 얹어만
   * 두면 공이 튈 때 열린다.
   */
  lidFlapMm: 8,
  /**
   * 옆벽 위에서 안쪽으로 180° 접어 내리는 **겹**.
   *
   * 두 가지를 한다. 하나는 얇은 종이 벽의 윗머리를 두 겹으로 만들어 세워 두는
   * 것이고, 다른 하나는 **뒷벽에서 넘어온 모서리 탭을 물어 잠그는 것**이다.
   * 풀도 칼집도 쓰지 않는 이유가 이 겹 하나다.
   */
  hemMm: 8,
  /**
   * 모서리에서 **오려 내는 홈**의 깊이 (2026-09-08).
   *
   * 상자 모서리는 종이가 붙어 있는 채로는 접히지 않는다. 뒷벽에 달린 모서리 탭은
   * 아래로 **옆벽**과, 위로 **뚜껑 귀**와 한 장으로 이어져 있어, 그대로 두면 벽을
   * 세우는 순간 서로를 잡아당긴다(사용자 지적) — 어느 하나를 접으면 나머지가
   * 딸려 온다.
   *
   * 그래서 탭 아래와 위에 이만큼 짧은 홈을 판다. 홈은 **바깥 윤곽의 일부**라
   * 따로 칼집을 낼 필요가 없다 — 오림선을 따라가다 홈으로 들어갔다 나오면 된다.
   * 6mm인 것은 가위 날이 들어가 방향을 바꿀 만한 최소 깊이여서다. 그만큼 탭이
   * 짧아지지만(24 → 18mm) 겹이 무는 윗머리 8mm는 그대로 덮는다.
   */
  cornerNotchMm: 6,
  /**
   * 뒷벽 양 끝에서 앞으로 접히는 **모서리 탭**.
   *
   * 옆벽 안쪽에 대고 그 위로 겹을 접어 내리면 뒷모서리가 닫힌다. 12mm는 겹
   * (8mm)에 물리고도 남는 길이다. 높이는 `cornerNotchMm`만큼 짧아져 18mm다.
   */
  cornerTabMm: 12,
} as const;

/** 전개도 한 벌의 외곽 크기. `artwork/goals.ts`가 이 값으로 배치한다. */
export const GOAL_NET_SIZE = {
  widthMm: (GOAL.hemMm + GOAL.wallHeightMm) * 2 + GOAL.mouthWidthMm, // 134
  heightMm:
    GOAL.lidDepthMm + GOAL.wallHeightMm + GOAL.trayDepthMm + GOAL.lipDepthMm, // 108
} as const;

/**
 * 운동장에 찍는 **골대 자리 가이드** (2026-09-05 사용자 요청).
 *
 * 골대가 골라인 밖에 놓이므로 종이 위에 놓을 자리가 보이지 않는다. 눈대중으로는
 * 가운데 맞추기가 어렵다 — 그래서 골포스트가 설 두 지점과 골문 한가운데를
 * 골라인에 걸친 짧은 눈금으로 찍는다. 쟁반이 된 뒤로는 **입술의 좌우 끝**을
 * 여기에 맞춘다(2026-09-06).
 *
 * **눈금이 전부다.** 골대 바닥 넓이를 파선 상자로 그리던 때는 골 에어리어 안에
 * 사각형이 하나 더 겹쳐 골라인 근처가 삼중선이 됐다. 맞출 것은 좌우 끝뿐이고,
 * 뒤로 얼마나 나가는지는 맞출 필요가 없다.
 */
export const GOAL_GUIDE = {
  /** 눈금이 필드 **안쪽**으로 들어오는 길이. 놀 면을 어지럽히지 않을 만큼만. */
  insideMm: 2,
  /** 눈금이 골라인 **바깥**으로 나가는 길이. 종이 끝(6mm)에서 2mm 남긴다. */
  outsideMm: 4,
  /** 골문 한가운데 표시. 좌우 눈금보다 짧게 두어 골포스트와 헷갈리지 않는다. */
  centerInsideMm: 2.5,
} as const;

/**
 * 필드 안 표시. 실제 축구장 치수를 약 **2.85mm/m**로 줄인 값이되, 좌표가 소수로
 * 흩어지지 않게 정수로 맞췄다(필드 285mm = 100m 기준).
 */
export const FIELD_MARKS = {
  /** 센터서클 반지름 (실제 9.15m). */
  centerCircleRadiusMm: 26,
  /** 센터 스팟·페널티 스팟 지름. */
  spotDiameterMm: 1.6,
  /**
   * 페널티 에어리어 — 골라인에서의 깊이와 폭 (실제 16.5m × 40.32m).
   *
   * 깊이는 실제 축척 그대로다. 폭만 115 → 140으로 넓혔다(2026-09-06) — 골 에어리어가
   * 골문을 따라 102mm로 커지면서 두 선 사이가 6mm밖에 남지 않았기 때문이다.
   * 골 에어리어 좌우로 19mm씩 남긴다. 필드 높이 198mm의 71%라 큼직하지만, 골대가
   * 실제보다 3배 넘게 과장된 판에서는 이쪽이 눈에 맞는다.
   */
  penaltyAreaDepthMm: 47,
  penaltyAreaWidthMm: 140,
  /**
   * 골 에어리어 = **골키퍼가 서는 자리**. 깊이는 실제 5.5m 그대로다.
   *
   * 골대가 골라인 안쪽을 16mm 깔고 앉던 때는 골키퍼 마커가 그 앞에 서야 해서
   * 깊이를 32mm로 늘렸었다 — 실제의 2배다. 골대를 골라인 위로 옮겨 필드 안
   * 공간을 비우면서 실제 축척(5.5m ≈ 16mm)으로 되돌렸다(2026-09-05).
   *
   * 폭은 **지금 골문 폭에서 잡는다**(2026-09-06). 골문이 70mm로 커지면서 예전
   * 78mm 안에 골대가 거의 꽉 차 골포스트와 골 에어리어 선이 겹쳐 보였다.
   * 골포스트 좌우로 16mm씩 남겨 102mm로 넓혔다 — 실제 정의(골포스트에서 좌우로
   * 5.5m)를 이 축척으로 옮긴 값이다.
   */
  goalAreaDepthMm: 26,
  goalAreaWidthMm: 102,
  /** 페널티 스팟까지의 거리 (실제 11m). */
  penaltySpotDistanceMm: 31,
  /** 코너 아크 반지름 (실제 1m). */
  cornerArcRadiusMm: 3,
  /** 필드 라인 굵기. 표시선(오림·접기)과 달리 배율을 같이 먹는다. */
  lineWidthMm: 0.5,
} as const;

/**
 * 선수 마커 (IDE-010) — 빈 원 / 일러스트 두 벌.
 *
 * 기준점은 두 벌 다 **마커 중심**이라 바꿔 끼워도 슬롯 좌표가 어긋나지 않는다.
 * 골키퍼는 같은 크기의 별도 스타일 세트(`goalkeeper-marker`)를 쓴다 — 크기는
 * 필드 선수와 같게 두어 겹침 판정과 프리셋 좌표가 그대로 통한다.
 *
 * ## 크기를 20mm로 잡은 근거 (2026-09-05)
 *
 * 12 → 15 → 24 → **20mm**. 24mm는 사용자가 어릴 때 가지고 놀던 종이 축구판
 * 사진에서 잰 값이었는데(선수 그림이 필드 짧은 변의 약 1/8), 실제로 그려 놓고
 * 보니 커서 20mm로 되돌렸다(사용자 요청).
 *
 * 6–7세 아이가 원 안에 번호를 쓰고 색칠하는 판이라(사용자 요청) 손이 들어갈
 * 만해야 한다는 요구는 그대로다.
 *
 * 20mm는 **상한이 아니라 취향에 맞춘 값**이다. 지금 배치가 허용하는 상한은
 * 24mm이고, 그 여유는 두 곳에 남아 있다:
 *
 * - 레인 사이 최소 간격이 **26mm**다. 24mm를 담으려고 벌려 놓은 값이라 20mm에는
 *   6mm가 남는다. 되돌리지 않은 것은 마커 사이가 넉넉한 편이 아이 손에 낫기
 *   때문이다.
 * - 골 에어리어 깊이가 **26mm**다. 골키퍼 레인 x=19에서 `19 + 10 = 29`이므로
 *   3mm가 남는다.
 *
 * 일러스트는 18×22다. 원보다 조금 작게 둔다 — 두 변형은 같은 슬롯에 바꿔 끼우는
 * 것이라 크기가 비슷해야 배치 인상이 유지되고, 그림이 세로로 길어 원과 폭을
 * 맞추면 세로가 먼저 필드를 벗어난다. 겹침 판정은 둘 중 큰 쪽(원 20mm)이다.
 */
export const PLAYER_MARKER = {
  circle: { widthMm: 20, heightMm: 20, valueFontSizeMm: 7 },
  illustration: { widthMm: 18, heightMm: 22, valueFontSizeMm: 7 },
} as const;

/**
 * 선수 그림의 자세 (2026-09-06)
 *
 * 옛 인쇄본처럼 선수마다 다른 자세로 서게 한다 — 한 그림을 스물두 번 찍으면
 * 판이 심심하다(사용자 요청). 자세는 사용자가 고르는 값이 **아니고** 슬롯에
 * 역할로 배정된다(`./index.ts`의 `POSE_BY_PLAYER`).
 *
 * 여기에는 id와 이름만 둔다 — 관절 각도는 아트워크 생성기가 갖는다
 * (`./artwork/player-markers.ts`의 `POSE_ANGLES`). 도안 정의는 스타일 세트
 * id와 파일 이름을 지으려고 이 목록만 읽으면 되고, 그림 그리는 코드가 앱
 * 번들에 딸려 들어가지 않는다(`artworkPath`와 같은 이유다).
 */
export const MARKER_POSES = [
  { id: 'run', label: '달리기' },
  { id: 'sprint', label: '질주' },
  { id: 'strike', label: '슛' },
  { id: 'pass', label: '패스' },
  { id: 'header', label: '헤딩' },
  { id: 'block', label: '수비' },
] as const;

/** 골키퍼 자세. 필드 선수와 세트를 따로 쓰므로 목록에서 뺐다. */
export const GOALKEEPER_POSE = { id: 'save', label: '세이브' } as const;

/**
 * 자세가 쓰는 마커 스타일 세트 id.
 *
 * 마커 아트워크는 변형에, 변형은 세트에 붙는다. 그래서 슬롯마다 다른 그림을
 * 쓰려면 **자세마다 세트를 나누는 수밖에 없다**(`docs/game-authoring.md`).
 * 세트가 달라도 변형 id·크기는 같아 슬롯끼리 바꿔 끼워도 배치가 어긋나지 않는다.
 */
export const poseStyleSetId = (poseId: string): string =>
  poseId === GOALKEEPER_POSE.id
    ? 'goalkeeper-marker'
    : `player-marker-${poseId}`;

/** 스타일 세트 × 변형 → 아트워크 파일 id. 생성기와 도안 정의가 같이 읽는다. */
export const markerArtworkId = (poseId: string, variantId: string): string =>
  `${poseStyleSetId(poseId)}-${variantId}`;

/**
 * 전술 대형이 쓰는 x 레인 (IDE-010).
 *
 * **골키퍼를 뺀 열 명이 자기 진영에 다섯, 상대 진영에 다섯 선다**(2026-09-06
 * 사용자 요청). 그래서 필드를 반으로 가르면 어느 쪽이든 홈 다섯 · 원정 다섯 ·
 * 골키퍼 하나가 있다. 홈은 여기 적힌 x를, 원정은 `BOARD.widthMm - x`를 쓴다
 * (`mirrorPositions`).
 *
 * 레인은 열둘이고 홈·원정이 번갈아 쓴다. 홈은 여섯 쌍(a, 297−a)에서 하나씩
 * 골랐다 — 자기 진영에 골키퍼·수비·수비형 중원, 상대 진영에 중원·공격형
 * 중원·공격:
 *
 *     홈    19  45        114       160       206  229
 *     원정        68  91       137       183       252  278
 *                              ↑ 하프라인 148.5
 *
 * 이웃 레인은 23mm 떨어진다(양 끝만 26). 마커 폭 20mm보다 넓어 두 팀의 y가
 * 같아도 겹치지 않는다 — 이것이 "어떤 대형 조합을 골라도 겹치지 않는다"의
 * 근거이고 `__tests__/formations.test.ts`가 지킨다. 더 벌리고 싶어도 자리가
 * 없다: 골키퍼 사이 259mm에 레인 열둘이 들어가야 해서 간격 23이 상한이다.
 * 마커를 키우면(2026-09-05에 24mm까지 갔었다) 이 배치는 깨진다.
 *
 * 자기 진영 레인 둘 중 안쪽(수비형 중원)의 마커 오른쪽 끝이 124, 상대 진영
 * 첫 레인(중원)의 왼쪽 끝이 150이라 어느 마커도 하프라인을 물지 않는다.
 */
export const FORMATION_LANES = {
  /**
   * 골키퍼. 골문 바로 앞이면서 골 에어리어(깊이 26mm) 안이다 —
   * `19 + 10 = 29`가 골 에어리어 끝(32) 안이다.
   */
  goalkeeper: 19,
  /** 수비 — 자기 진영. */
  defence: 45,
  /** 수비형 중원 — 자기 진영. 대형마다 자기 진영 다섯을 채우는 한둘이 선다. */
  defensiveMidfield: 114,
  /** 중원 — 상대 진영 첫 레인. */
  midfield: 160,
  /** 공격형 중원 — 4-2-3-1의 3. */
  attackingMidfield: 206,
  /** 공격. */
  forward: 229,
} as const;

/** 원정 팀이 쓰는 레인. 홈 레인을 세로 중심선 기준으로 뒤집은 값이다. */
export const awayLaneXMm = (homeLaneXMm: number): number =>
  BOARD.widthMm - homeLaneXMm;

/**
 * 공 — 연필로 튕기는 납작한 원.
 *
 * 예전에는 24개짜리 **공 마커 시트**를 함께 뽑았지만 그 파트를 출력물에서 뺐다
 * (2026-09-05 사용자 요청). 지름은 남는다 — 골문 크기를 정한 근거가 이 값이고
 * (`지름 × 2 < 골문 폭`), 준비물 안내에도 "지름 12mm쯤"으로 나간다.
 */
export const BALL = {
  /** 오리기 쉽고 튕기기 좋은 크기로 잡았다(⚠︎ 종이 실측 대기). */
  diameterMm: 12,
} as const;

/** 부속 파트 크기. */
export const SHEETS = {
  /** 점수 기록칸 — A5 가로. */
  scoreSheet: { widthMm: 210, heightMm: 148.5 },
  /**
   * 골대 전개도 **두 벌** + 그림 도해 — A4 가로 한 장에 딱 들어가는 크기.
   *
   * 뚜껑이 붙으면서(2026-09-08) 전개도 한 벌이 134×108mm가 됐다. 나란히 놓으면
   * 268mm라 접는 법 글까지 얹으면 한 벌밖에 못 앉힌다. 그래서 **글을 소개
   * 페이지로 옮겼다**(사용자 요청) — 시트에는 그림 도해 넉 장만 남고, 비워진
   * 폭에 두 벌이 나란히 들어간다. 골대 둘에 종이 한 장이다.
   *
   * A5(210×148.5)였다. 골문이 사진 크기로 커지면서 전개도가 A5를 넘었다.
   *
   * 그렇다고 운동장처럼 297×210(=A4 그 자체)으로 두지는 않는다. **인쇄 여백은
   * 사용자가 고르는 값**이고(`marginMm`, 기본 0), 여백을 조금만 주면 297×210짜리
   * 파트는 곧바로 두 장으로 쪼개진다 — 운동장이 여백 5mm에서 A4 2장이 되는 것이
   * 그래서다. 조립물이 두 장으로 쪼개지면 이어 붙인 자리에서 전개도가 어긋난다.
   *
   * 280×190은 **여백 8mm까지 한 장을 지킨다.** 남는 자리는 하단 표식 띠(7mm)
   * 몫으로도 쓰인다 — 띠가 도안을 덮지 않는다.
   */
  goals: { widthMm: 280, heightMm: 190 },
} as const;

/** 점수 기록칸 표 배치. 슬롯 좌표(`./index.ts`)가 이 값에서 나온다. */
export const SCORE_TABLE = {
  /** 오림선을 시트 가장자리에서 얼마나 안쪽에 두는지. */
  cutInsetMm: 5,
  xMm: 15,
  headerYMm: 26,
  headerHeightMm: 12,
  rowHeightMm: 8,
  rows: 12,
  /** 판 번호 열 너비. 나머지를 두 팀이 반씩 나눈다. */
  indexColumnMm: 30,
  teamColumnMm: 75,
} as const;

export const SCORE_TABLE_WIDTH_MM =
  SCORE_TABLE.indexColumnMm + SCORE_TABLE.teamColumnMm * 2; // 180

/**
 * 헤더 행 글자의 세로 중심.
 *
 * 예전에는 헤더 위에 팀 색 막대(2.5mm)를 얹고 그 아래 남는 칸의 가운데였다.
 * **막대를 뺐다**(2026-09-08 사용자 요청 — "점수 기록도 파랑 빨강 필요없이,
 * 판/이름으로 빈칸으로") — 이제 헤더 칸 전체의 가운데다.
 */
export const SCORE_HEADER_Y_MM =
  SCORE_TABLE.headerYMm + SCORE_TABLE.headerHeightMm / 2;

/**
 * 파트 id에 대응하는 정적 자산 경로.
 *
 * 도안 정의가 여기서 경로를 받는다 — 아트워크 생성기(`./artwork/`)에서 받아 오면
 * SVG를 짓는 코드가 통째로 앱 번들에 딸려 들어간다.
 */
export const artworkPath = (partId: string): string =>
  `/games/soccer/${partId}.svg`;
