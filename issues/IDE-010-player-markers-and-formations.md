# IDE-010 · 선수 마커 아트워크와 전술 대형 프리셋

| 항목        | 값                        |
| ----------- | ------------------------- |
| 상태        | `done`                    |
| 영역        | content                   |
| 우선순위    | P0                        |
| 마일스톤    | M1                        |
| 추정        | 5d                        |
| 추천 모델   | Sonnet 5                  |
| 추천 Effort | medium–high               |
| 선행        | ~~IDE-003~~ · ~~IDE-004~~ |
| 후행        | `IDE-006` · `IDE-012`     |

## 배경

축구 게임판에서 선수는 도안 위에 놓이는 별개의 층이다. 필드는 고정이지만 선수는
스타일도 위치도 사용자가 고른다. 그래서 필드 도안(`IDE-004`)과 분리해 만든다.

두 가지가 필요하다(2026-09-03 확정):

- **마커 스타일 두 벌** — ① 원 + 등번호 ② 선수 일러스트 + 등번호. 사용자가 고른다.
  ①은 사용자가 직접 만든 판의 방식이고, ②는 옛 인쇄본의 방식이다
- **전술 대형 프리셋** — 4-4-2, 3-5-2 같은 실제 축구 포메이션대로 11명을 배치한다.
  프리셋은 출발점이고 여기서 자유롭게 옮길 수 있다(`IDE-006`)

한 팀은 **골키퍼 포함 11명**, 양 팀 22명이다. 사용자가 만든 판 사진에는 골키퍼가
빠져 있었지만 정식 구성은 11명이다.

일러스트는 옛 인쇄본을 복제하지 않고 직접 만든다.

## 할 일

- [x] 원형 마커 세트 작도 — 원 + 등번호, 팀 색 적용 가능하게, 골키퍼는 구분되게 ·
      `player-marker-circle.svg` / `goalkeeper-marker-circle.svg`. 팀 색은
      `pc-marker-fill` 레이어가 받고, 골키퍼는 안쪽 테 하나로 구분된다.
- [x] 일러스트 마커 세트 제작 — **전원 동일 포즈**로 결정했다(달리는 자세
      픽토그램 하나, 골키퍼만 장갑 표식 추가). 포지션별 포즈나 몇 가지 포즈
      돌려쓰기는 하지 않는다 — 근거는 결정 기록.
- [x] 두 세트가 같은 슬롯에 교체 가능하도록 기준점·크기 규격을 맞춘다 · 원형
      12×12mm, 일러스트 13×16mm로 필드 선수·골키퍼 세트가 크기를 공유한다
      (`PLAYER_MARKER`, `dimensions.ts`). 기준점은 두 세트 다 마커 중심이다.
- [⚠︎] 등번호 가독성 확인 — 일러스트 위에 얹었을 때, 그리고 축소 배율에서 ·
  `valueFontSizeMm`을 두 세트 다 5mm로 맞춰 필드 `minScale`(0.5)에서
  2.5mm 하한에 닿는 것은 테스트로 확인했다. **일러스트 실루엣 위에 실제로
  얹었을 때 읽히는지는 렌더러가 없어(`IDE-006`) 눈으로 못 봤다.**
- [x] 전술 대형 프리셋 좌표 데이터 작성 — 최소 4-4-2 · 3-5-2 · 4-3-3 · 4-2-3-1 ·
      네 대형 모두 `FORMATIONS`에 있고 홈·원정 프리셋이 자동 생성된다.
- [x] 각 프리셋의 기본 등번호 배정 (GK 1번 등 통념을 따르되 사용자가 바꿀 수 있다) ·
      슬롯 id가 대형과 무관하게 고정이라(`home-player-1`…11) 어느 프리셋을 골라도
      1번 = 골키퍼가 유지된다. 기존 IDE-004 구현을 그대로 확인했다.
- [x] 프리셋 좌표를 양 팀 진영에 대칭으로 적용하는 방식 정의 · `mirrorPositions`로
      세로 중심선 반전(기존 IDE-004 구현), 새 대형 둘에도 그대로 적용했다.
- [x] 선수끼리 겹치거나 필드 밖으로 나가지 않는지 프리셋 전부 확인 · `parseGame`이
      네 대형 × 홈/원정 조합 전부를 좌표·겹침·영역으로 검증하고, 홈 4벌 × 원정
      4벌 = 16개 조합을 도는 회귀 테스트(`registry.test.ts`)로 다시 확인했다.
- [⚠︎] 흑백 출력에서 두 팀이 구분되는지 확인 (색만으로 구분하면 안 된다) ·
  마커 오른쪽에 색과 무관한 화살촉(공격 방향)을 붙이고 원정은 좌우 반전해
  쓰기로 설계했다 — 화살촉이 기하로 비대칭임은 테스트로 확인했다. **원정
  마커를 실제로 반전해 찍는 것과, 인쇄물에서 눈으로 구분되는지는 렌더러가
  없어(`IDE-006`·`IDE-007`) 검증하지 못했다.**

## 수용 기준

- [x] 원형·일러스트 두 마커 세트가 모두 있고 서로 바꿔 끼워도 배치가 어긋나지 않는다
- [x] 한 팀 11명(골키퍼 포함), 양 팀 22명이 도안에 배치된다
- [x] 전술 대형 프리셋이 4종 이상 있고 각각 실제 포메이션에 맞는 배치다
- [x] **두 팀이 필드 전체에 섞여 서서 경기가 성립한다** — 각 팀이 상대 진영에도
      선수를 두어 패스로 밀고 올라갈 수 있고, 최전방이 슛 사거리 안이다
      (2026-09-05 추가. 원래 수용 기준이 이 조건을 담지 못해 경기를 할 수 없는
      배치가 `done`으로 닫혔다 — 결정 기록 참고)
- [x] **골키퍼가 골대를 세운 뒤에도 골 에어리어 안에 선다**(2026-09-05 추가)
- [x] **골키퍼를 뺀 10명이 운동장 1/3마다 둘 이상 있다**(2026-09-05 추가)
- [x] 어떤 프리셋을 골라도 선수가 겹치거나 필드를 벗어나지 않는다 — **두 팀이
      서로 다른 대형을 고른 조합까지** 포함한다(2026-09-05에 조건을 넓혔다)
- [x] 마커 아트워크가 색과 무관하게 양 팀·골키퍼를 구분하는 표식을 갖는다
      (팀: 방향 화살촉 · 골키퍼: 이중 테)이고, 그 표식이 기하로 비대칭임이
      테스트로 확인된다 · **범위 조정(2026-09-04, 원문: "흑백으로 출력해도 양
      팀과 골키퍼가 구분된다")** — 이 이슈는 렌더러 없이 아트워크와 스키마만
      만든다. 원정 마커에 화살촉 반전을 실제로 적용해 찍고, 인쇄물에서 눈으로
      구분되는지 확인하는 것은 렌더러를 만드는 `IDE-006`(→ 인쇄 결과는
      `IDE-007`)로 넘긴다.
- [x] 옛 인쇄본의 선수 일러스트를 복제하지 않았음이 문서로 확인된다

## 결정 기록

- 2026-09-03 · 마커를 원형과 일러스트 두 벌로 제공하기로 결정. 필드 도안과 분리해
  별도 이슈로 뺐다 — 성격이 다르고 `IDE-004`가 너무 커졌다.
- 2026-09-03 · 한 팀 11명(골키퍼 포함)으로 확정. 사진에 골키퍼가 없었던 것은 그리다
  뺀 것이지 구성이 아니다.
- 2026-09-04 · 선행 `IDE-004`를 취소선으로 풀었다. `IDE-004`는 아직 `blocked`지만
  기다리는 것(종이 실측 · 규칙 확인)이 이 이슈와 무관하고, 여기서 필요한 것은 다
  나왔다 — 필드 기하와 영역(`playable-field` 등), 마커 스타일 세트 규격, 4-4-2·3-5-2
  출발 좌표. 시작점은 [docs/soccer-artwork.md](../docs/soccer-artwork.md)다.
  다만 **골키퍼 좌표는 골대 자리(골라인 안쪽 16mm)를 침범하면 안 된다** — 대형을
  확정할 때 이 제약을 지켜야 하고, 테스트가 검사한다.
- 2026-09-04 · 일러스트 마커의 포즈 벌 수를 **전원 동일**로 정했다. 포지션별
  포즈는 22명 각각 다른 그림이 필요해 작업량이 이 이슈 하나로 감당하기 어렵고,
  게임에 포지션별 능력치 차이가 없어(등번호만 다르다) 포즈로 포지션을 구분할
  이유도 없다. 몇 가지 포즈를 돌려 쓰는 안도 검토했지만, 어느 포즈를 누구에게
  배정할지 정할 기준이 없어 임의성만 늘었다. 대신 **골키퍼는 예외로 장갑
  표식**을 더해 역할 하나만은 확실히 구분했다.
- 2026-09-04 · 흑백에서 팀·골키퍼를 구분하는 방법을 스타일 세트 분리 + 도형
  비대칭으로 정했다. 팀 색은 사용자가 자유 입력이라(`hexColor`) 색의 명도
  차이에 기댈 수 없다 — 그래서 (1) 필드 선수·골키퍼를 애초에 다른 스타일
  세트(`player-marker` / `goalkeeper-marker`)로 나눠 골키퍼는 이중 테로 항상
  다르게 그리고, (2) 팀은 마커에 방향성 있는 화살촉을 붙이고 원정을 좌우
  반전해서 찍기로 했다. 후자는 "찍을 때 반전한다"는 **렌더러 요구사항**이라
  이 이슈에서는 도형이 비대칭임을 기하로만 확인했고, 실제 반전 적용과 인쇄
  확인은 `IDE-006`·`IDE-007`로 넘긴다 — 그래서 관련 수용 기준을 위와 같이
  조정했다.
- 2026-09-04 · 전술 대형에 4-3-3 · 4-2-3-1을 추가해 4종을 채웠다. 골키퍼는
  모든 대형에서 x=32(골대 자리 밖), 나머지 선수는 x≥52로 둬 골대 자리 침범
  검사를 자동으로 통과하게 잡았다 — `parseGame`이 네 대형 모두를 통과시켰다.
- 2026-09-04 · 구현 확인 완료. 남은 것(⚠︎ 두 개)은 전부 이 이슈만으로는 끝낼 수
  없는 항목 — 렌더러(`IDE-006`) 또는 실물 인쇄(`IDE-002`와 같은 사유)가
  있어야 한다. 그래서 `review`로 올린다.
- 2026-09-04 · 재검증 후 `done`으로 닫는다. 수용 기준 6개 모두 충족(하나는
  범위를 조정해 충족)했고, 테스트(`npm run test:run` 104개)·lint·typecheck가
  전부 통과한다. 남은 ⚠︎ 두 개(일러스트 위 등번호 실제 가독성 · 흑백 인쇄에서
  눈으로 확인)는 렌더러가 필요한 항목이라 `IDE-006`·`IDE-007`로 넘어간다 —
  이 이슈의 수용 기준에는 들지 않는다.
- 2026-09-05 · **경기를 할 수 없는 배치였다. 대형을 다시 잡았다.**
  사용자가 지적해 드러났다 — "두 팀의 대형이 섞여야 경기를 할 수 있는데, 좌우
  절반을 각각 차지하고 있으면 경기를 할 수 없다."
  - 선수 마커는 운동장에 **인쇄되어 움직이지 않는다.** 규칙은 패스가 자기 팀
    선수에게 닿아야 이어지고 슛도 공이 자기 팀 선수 위에 있을 때만 된다. 두 팀이
    각자 진영 절반에 갇히면 상대 골대 쪽에 받아 줄 선수가 없어 **공을 앞으로
    보낼 방법이 아예 없다.** 홈 최전방이 x=130, 상대 골대가 x=273부터라 슛
    한 번에 143mm를 튕겨야 했다.
  - 실제 축구의 킥오프 배치를 그대로 옮긴 것이 원인이다. 실물 축구는 선수가
    움직이지만 이 게임의 선수는 종이에 박혀 있다는 차이를 놓쳤다.
  - x를 다섯 레인(`FORMATION_LANES`)으로 정하고 두 팀이 번갈아 서게 했다.
    레인 사이가 최소 20mm(마커 폭 13mm)라 **어떤 대형 조합에서도** 겹치지 않는다.
    최전방에서 상대 골대까지 68mm로 슛이 성립한다.
  - 수용 기준이 이 조건을 담지 못한 것이 더 큰 문제였다. "실제 포메이션에 맞는
    배치"는 만족했는데 경기가 안 됐다. **"경기가 성립하는가"를 수용 기준으로
    올리고** `__tests__/formations.test.ts`로 고정했다.
- 2026-09-05 · **스키마 검증이 그룹 사이 겹침을 보지 않고 있었다.** 프리셋 하나
  안의 겹침만 검사해서, 홈과 원정을 나란히 놓았을 때 부딪히는 경우를 놓쳤다.
  두 팀이 좌우로 갈려 있던 동안에는 드러나지 않던 구멍이다. 팀을 섞으면서
  실제 위험이 되어 `parseGame`에 **다른 그룹의 프리셋과의 겹침** 검사를 넣었다
  (`docs/game-authoring.md` 배치 프리셋 절).
- 2026-09-05 · **원정 마커 좌우 반전을 구현했다.** 이 이슈가 `IDE-006`·`IDE-007`로
  넘겼던 항목인데(수용 기준 5번의 범위 조정) 두 이슈 다 빠뜨린 채 닫혔다.
  두 팀이 섞여 서면 위치로 팀을 알 수 없어, 화살촉 방향이 **흑백에서 팀을 가르는
  유일한 단서**가 된다. 도안이 `group.mirrorMarkers`로 선언하고 미리보기와
  인쇄물이 같은 규칙으로 뒤집는다. 등번호는 뒤집지 않는다.
- 2026-09-05 · 규칙 카드의 "선수 마커를 오려 각자 진영에 놓는다"가 도안과 어긋나
  있었다. 마커는 오리는 부속이 아니라 **운동장에 인쇄된다.** "둘이 팀을 하나씩
  맡는다 · 마커 옆 화살표가 공격 방향"으로 고치고, 두 팀이 섞여 선다는 설명을
  더했다. 카드가 넘쳐 마지막 안내 한 줄은 뺐다.
- 2026-09-05 · 운동장이 넓어져(`IDE-004`, 터치라인 281×180 → 285×198) 레인과
  y 좌표를 다시 잡았다. 골키퍼 30 · 수비 68 · 중원 112 · 공격형 미드 160 ·
  공격 207이고, 원정은 그 대칭이라 레인 사이가 최소 22mm다(마커 폭 13mm).
  y도 넓어진 필드에 맞춰 벌려 마커가 운동장에 고루 퍼진다.
- 2026-09-05 · **골키퍼가 골 에어리어 밖에 서 있었다**(사용자 지적). 골키퍼는
  종이 골대를 세운 뒤 그 앞에 서는데, 골대가 골라인 안쪽 16mm를 차지하고 마커가
  13mm라 골 에어리어 깊이가 최소 29mm는 되어야 마커가 그 안에 들어간다. 실제
  축구의 5.5m(≈16mm)를 그대로 옮겨 20mm로 두었던 탓에 골키퍼가 자기 구역 밖에
  서 있었다. **골 에어리어를 32×80mm로 키웠다**(실제보다 크지만, 이 게임에서는
  골키퍼 자리를 표시하는 것이 그 선의 쓸모다).
- 2026-09-05 · **4-2-3-1만 운동장 1/3에 고르게 서지 않았다**(사용자 지적).
  공격형 미드필더 레인이 중원에 있어 상대 진영에 공격수 한 명만 남았다 —
  1/3마다 4/5/1이라 4-4-2(4/4/2)·4-3-3(4/3/3)보다 앞이 헐거웠다. 선수가 종이에
  박혀 움직이지 않으므로, 어느 구역에 사람이 없으면 그 구역에서는 패스를 이어
  갈 수 없다.
  - 레인을 다시 잡았다 — 골키퍼 30 · 수비 60 · 중원 135 · 공격형 미드 200 ·
    공격 220. **공격형 미드를 상대 진영으로 올려** 4-2-3-1이 4/2/4가 됐다.
    중원 레인도 112 → 135로 올려 센터서클 주변이 비지 않게 했다.
  - 원정 레인이 홈의 대칭이라 자유도가 낮다. 홈 레인끼리의 합이 297에 가까우면
    홈 선수와 원정 선수가 부딪히는데, 이 조합의 최소 여유는 17mm다(마커 13mm).
  - "1/3마다 둘 이상"을 수용 기준으로 올리고 테스트로 고정했다.

- 2026-09-05 · **마커를 빈 원으로 바꾸고 15mm로 키웠다**(사용자 요청). 이 판이
  **6–7세 아이와 하는 것**이고, 번호를 직접 쓰고 색칠하는 것 자체가 놀이의
  일부라는 전제가 새로 들어왔다.
  - 바꾼 셋: ① 원 채움을 없앴다(팀 색 → 흰 속 + 잉크 테두리 0.7mm) ② 등번호
    기본값을 비웠다 ③ 지름을 12 → **15mm**로 키웠다.
  - **팀 색 슬롯을 통째로 뺐다.** 마커에서 색이 빠지면 남는 쓸모가 점수
    기록칸의 색 막대 하나뿐인데, 그 막대도 아이가 칠하는 자리로 돌리는 편이
    일관된다. 남겨 두면 **에디터에서 고른 색이 인쇄물 어디에도 안 나와** 화면이
    거짓말을 한다. `group.colorSlotId`도 함께 비웠다.
    - 흑백 대비 경고(`IDE-009`)는 코드에 그대로 뒀다. 색 슬롯이 없는 게임에서는
      비교할 색이 없어 저절로 조용해지므로 죽은 코드가 아니고, 다른 게임이 색을
      쓰면 그대로 작동한다.
  - **등번호 슬롯을 `number` → `text`로 바꿨다.** `number`는 빈 값을 받지 못하는데
    이 도안의 기본값은 비어 있어야 한다. 값이 비면 렌더러가 글자를 아예 얹지
    않는다 — 미리보기와 인쇄가 같은 규칙을 쓴다.
  - 15mm의 상한은 둘이 정했다: 레인 사이 최소 간격 **17mm**(넘으면 두 팀 마커가
    겹친다)와 골키퍼가 골 에어리어(깊이 16mm) 안에 들어가야 한다는 조건
    (x=14에서 `14 + 폭/2 ≤ 22` → 16mm). 15mm는 둘 다 여유를 조금 남긴다.
  - **화살촉도 같이 키웠다**(돌출 1.1 → 1.5mm, 각도 ±22° → ±26°). 채움이
    사라진 뒤로 이 삼각형이 팀을 가르는 **유일한** 표식이 됐는데, 원만 키우니
    상대적으로 작아져 방향이 읽히지 않았다.
  - 채움을 `none`이 아니라 **흰색**으로 둔 것은 밑에 깔린 필드 라인이 원을
    가로지르면 색칠할 면이 지저분해지기 때문이다.
  - 일러스트 변형은 13×16 그대로다. 채워진 픽토그램이라 색칠할 면이 아니고,
    겹침 판정은 둘 중 큰 쪽(원 15mm)으로 하므로 안전하다. 팀 색을 받던 레이어가
    빠져 실루엣은 잉크색 하나로 그린다.
  - 등번호 글자는 5 → 6mm. 커진 원에 맞췄고, 필드 `minScale`(0.5)에서 3mm라
    가독성 하한(2.5mm)에 처음으로 여유가 생겼다.
  - ⚠︎ **지름 15mm가 6–7세 아이 손에 실제로 충분한지는 확인하지 못했다.** 좁으면
    레인 간격과 골 에어리어 깊이를 함께 늘려야 원이 더 커질 수 있다.
- 2026-09-05 · **마커를 24mm로 다시 키우고 레인을 재배치했다.** 15mm로 올린
  직후 사용자가 **어릴 때 가지고 놀던 종이 축구판 사진**을 보여 주며 "이 정도
  비율"이라고 했다.
  - 그 판에서 선수 그림은 **필드 짧은 변의 약 1/8**을 차지한다. 우리 필드 짧은
    변이 198mm이므로 약 24mm — 15mm는 그 절반 남짓이었다.
  - 24mm는 **제약이 허용하는 상한**이다. 상한을 역산해 보니 두 가지가 막았다:
    - 레인 사이 최소 간격 17mm. 마커가 그보다 크면 두 팀 마커가 겹친다.
      → 레인을 다시 잡아 **26mm**로 벌렸다. 간격 다섯이 대칭이라
      (`d−g`, `297−f−d`, `f−a`, `m+a−297`, `297−2m`) 그 값들만 26 이상이면
      열 레인 전체가 안전하다. 홈 레인 `14·60·135·200·220` →
      **`19·45·124·199·225`**.
    - 골키퍼가 골 에어리어 안에 들어가야 한다. `19 + 12 = 31`이므로 깊이가
      26mm는 되어야 한다 → 골 에어리어 16×71 → **26×78**(폭은 깊이의 3배로,
      실제 가로세로비 3.3에 가깝다).
  - 대형별 1/3 분포(4/4/2 · 3/5/2 · 4/3/3 · 4/2/4)와 최전방 슛 사거리는 새
    레인에서도 그대로다. y 좌표는 손대지 않았다 — 같은 레인 안 최소 간격이
    37mm(3-5-2 중원)라 24mm 마커에 여유가 있다.
  - 일러스트는 13×16 → **22×27**. 원과 같은 24로 두면 세로가 30mm를 넘어 대형
    y의 위아래 끝이 필드를 벗어난다. 그림 좌표를 하나씩 고치는 대신 캔버스
    배율 함수(`fx`·`fy`)를 넣었다 — 다음에 크기를 바꿀 때는 숫자 두 개만 고친다.
  - 등번호 글자 6 → 7mm.
  - 작도 중 잡은 것: `artwork.test.ts`의 마커 폭이 **13mm로 박혀 있었다.**
    실제로 12 → 15 → 24mm로 커지는 동안 그대로여서, "마커가 골라인을 넘지
    않는가" 검사가 계속 느슨하게 통과하고 있었다. `PLAYER_MARKER`에서 읽도록
    고쳤다.
- 2026-09-05 · **팀 색을 마커 테두리로 되살렸다**(사용자 요청 — "원의 배경색은
  없지만 원의 색상은 빨강과 파랑으로 구분해줘").
  - 몇 시간 전 마커를 빈 원으로 바꾸면서 팀 색 슬롯을 뺐었다. 채움이 사라져
    색이 갈 곳이 없다고 봤는데, **테두리라는 자리가 남아 있었다.** 원 안은
    여전히 비어 있으므로 아이가 칠할 면은 그대로다.
  - 슬롯을 복원했다(홈 `#1d4ed8` 파랑 · 원정 `#dc2626` 빨강). 점수 기록칸의
    색 막대도 다시 채워진다 — 종이에서 "파랑 팀 = 파란 테두리 마커"가 맞물린다.
  - 레이어 규약을 하나로 합쳤다. `pc-marker-fill`·`pc-marker-stroke`를 따로 두는
    대신 **`pc-marker-team` 하나에 `fill`과 `stroke`를 둘 다 칠한다.** 색이 붙는
    자리는 도안이 그린 도형이 정한다 — 빈 원은 테두리·화살촉이 받고(속은
    `fill="#ffffff"`로 못 박아 덮어쓴다), 일러스트는 실루엣이 채움으로 받는다.
  - 스키마의 마커 변형에 **`filled`**를 더했다. 이 값이 **등번호 색을 가른다** —
    채워진 마커는 배경 밝기에 맞춰 반전(`readableTextColor`), 빈 원은 배경이 흰
    종이라 팀 색 그대로. 반대로 두면 파란 테두리 원 안에 흰 글자가 찍혀 사라진다.
    빈 원 변형에 `filled: false`를 선언했다.
  - 작도 중 잡은 것: 화살촉을 처음에 `stroke`로만 그렸더니 **속이 빈 삼각형**이
    나왔다. 레이어가 `fill`도 받게 하고 화살촉은 면으로 되돌렸다.
  - 테스트 하나가 **우연히 통과하고 있었다.** "팀 색 레이어가 없다"를 검사하는데
    레이어 id를 `pc-marker-team`으로 바꿔 놓아 옛 id(`pc-marker-fill`)로는
    여전히 없었다. 뜻이 반대가 된 검사라 "레이어가 있고 비어 있지 않다"로
    고치고, 빈 원의 흰 채움과 일러스트의 색 채움을 각각 확인하게 했다.
- 2026-09-05 · **마커 원을 24 → 20mm로 되돌렸다**(사용자 요청). 24mm는 어릴 때
  판 사진에서 잰 값이었는데 실제로 그려 놓고 보니 컸다.
  - 레인 간격(26mm)과 골 에어리어 깊이(26mm)는 **그대로 뒀다.** 24mm를 담으려고
    벌려 둔 값이라 20mm에는 각각 6mm·3mm가 남는데, 되돌리면 필드 그림이 또
    움직이고 남는 여유가 해가 되지 않는다. 지금 배치는 24mm까지 담을 수 있다.
  - 일러스트도 22×27 → **18×22**로 함께 줄였다. 원만 줄이면 일러스트가 더 커져
    두 변형의 크기 관계가 뒤집히고, 겹침 판정이 일러스트 기준이 되어 원을 줄인
    효과가 반감된다.
- 2026-09-06 · **선수 그림이 자기 골대를 보고 있었다**(사용자 지적 — "파랑팀,
  빨강팀 모두 자신의 골대 방향으로 몸이 돌아가 있어").
  - 원인은 좌우 반전이 아니라 **그림 자체**였다. 손으로 찍은 좌표 한 벌이었고,
    크게 뻗은 팔다리를 "앞으로 내딛는" 쪽으로 그렸는데 사람 눈에는 차고 나온
    뒷다리로 읽혔다. 반전 로직(`mirrorMarkers`)과 화살촉은 처음부터 맞았다.
  - 그림을 좌우로 뒤집어 급히 막았고, 아래 자세 작업에서 무릎·팔꿈치 각도를
    따로 주는 뼈대로 바꾸면서 근본 원인을 없앴다 — 굽힌 앞다리와 뻗은 뒷다리가
    구분되고 발끝이 방향을 못 박는다.
- 2026-09-06 · **선수마다 자세를 다르게 했다**(사용자 요청 — 옛 인쇄본 사진을
  다시 보여 주며 "좀더 다양하게 다이나믹하게").
  - 그림을 **뼈대에서 짓는다.** `POSE_ANGLES`가 관절 각도(상체 기울기·팔꿈치·
    무릎)만 적고 `figureShapes`가 도형을 낸다. 자세를 더할 때 도형이 아니라
    각도 여덟 개를 적는다. 지금은 달리기·질주·슛·패스·헤딩·수비 여섯에 골키퍼
    세이브 하나다.
  - **자세마다 스타일 세트를 나눴다.** 아트워크는 변형에, 변형은 세트에 붙어
    세트 하나로는 슬롯마다 다른 그림을 낼 수 없다. 세트 전부가 같은 변형
    구성·크기를 가지므로 겹침 판정과 프리셋 좌표는 그대로다. 배정은 기본
    대형(4-4-2)의 역할을 따른다(`POSE_BY_PLAYER`) — 사용자가 고르는 값이 아니다.
  - **색칠용 윤곽선 변형(`outline`)을 더했다**(같은 요청의 뒷부분 — "아이가
    색칠할수있게 옵션을 제공하면 윤곽선만 칠하게 해줘"). 실루엣과 **같은
    도형**을 속만 비워 낸다. 몸을 부위마다 닫힌 도형으로 내는 구조라 한 뼈대에서
    두 벌이 나온다 — 실루엣에서는 이음매가 안 보이고, 윤곽선에서는 그 경계가
    팔·다리 선이 된다.
  - **선수 그림에서 화살촉을 뺐다.** 자세가 이미 비대칭이라 발밑 삼각형이 몸이
    가리키는 방향과 경쟁했다. 빈 원은 대칭이라 그대로 둔다. 대신 테스트가
    "뒤집었을 때 얼마나 어긋나는가"로 자세의 비대칭을 잰다.
  - 작도 중 잡은 것: 질주 자세가 **마커 상자를 1mm 넘었다.** 각도를 손으로
    맞추는 대신 `fitToBox`를 넣어 도형 경계를 재서 줄이고 가운데로 옮긴다 —
    자세를 더할 때 상자 걱정을 하지 않아도 된다. 상자를 넘으면 이웃 마커와
    겹친다(겹침 판정은 상자로만 한다).
  - 옛 파일 `player-marker-illustration.svg`는 지웠다. 이름이 자세를 담게
    바뀌었다(`player-marker-<자세>-<모양>.svg`).
