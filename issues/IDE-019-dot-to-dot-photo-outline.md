# IDE-019 · 점 잇기 — 사진에서 윤곽선을 따고 점·번호를 찍는다

| 항목        | 값                    |
| ----------- | --------------------- |
| 상태        | `review`              |
| 영역        | content               |
| 우선순위    | P1                    |
| 마일스톤    | M2                    |
| 추정        | 4d                    |
| 추천 모델   | Opus 5                |
| 추천 Effort | xhigh                 |
| 선행        | —                     |
| 후행        | `IDE-020` · `IDE-021` |

## 배경

다섯 번째 놀이다. 사용자가 요청했다(2026-09-08).

**사진을 넣으면 윤곽선을 따고, 그 선 위에 원하는 개수만큼 점을 찍고, 점마다
번호를 적은 도안을 인쇄한다.** 아이는 1부터 차례로 선을 이어 그림을 완성한다.
목적이 둘이다 — **숫자를 순서대로 읽히는 것**과 **연필을 쥐고 선을 긋는 운필력**.

앞의 네 게임과 다른 점이 이것이다. 축구·야구·세계일주·윷놀이는 **도안이 저장소
안에 있고** 사용자는 이름·색·마커 자리만 고쳤다. 점 잇기는 **인쇄물의 형태 자체가
사용자가 넣은 사진에서 나온다.** 저장소에 그려 둘 그림이 없다.

이 저장소에 맞는 놀이이기도 하다. 결과물이 종이 한 장이고, 연필만 있으면 되고,
같은 사진으로 점 개수만 바꿔 다시 뽑으면 난이도가 달라진다 — 인쇄를 여러 번 하게
만드는 것이 이 사이트가 하는 일이다.

## 이 놀이가 규격에서 처음 밟는 것

**인쇄물이 실행 시점에 생성된다.** 지금 파트의 `artwork`는
`public/games/<게임 id>/*.svg`를 가리키는 정적 경로이고(`src/lib/schema/parts.ts`의
`assetRef`), 그 파일을 읽는 자리가 둘이다.

- 미리보기 — `src/components/editor/BoardPreview.tsx`가 `fetch(part.artwork)` 한다.
- 내보내기 — `src/app/api/games/[id]/export/route.ts`의 `loadArtwork`가
  `public/` 아래에서 `readFileSync` 한다.

점 잇기는 사진마다 도안이 다르므로 **가리킬 파일이 없다.** 다행히 두 자리 모두
"경로 → SVG 원문" 한 함수로 좁혀져 있고(`composeExport`는 `LoadArtwork`를 인자로
받는다), 그 자리에 생성된 SVG를 넣으면 나머지 인쇄 파이프라인은 손대지 않아도
된다. **접점을 이 둘로 유지하는 것이 이 이슈에서 가장 되돌리기 비싼 결정이다** —
게임 정의가 SVG를 직접 들고 다니게 만들면 다른 네 게임의 정적 자산 규약이 함께
흔들린다.

**산출물이 순수 벡터라 나머지는 그대로 탄다.** 점 잇기 도안에 들어가는 것은 원과
숫자와 옅은 선뿐이다 — `parseArtwork`가 이미 읽는 요소다. 사진 자체는 **인쇄물에
넣지 않는다.** 파서가 `<image>`를 모르기도 하지만, 그보다 이을 선이 이미 종이에
보이면 아이가 번호를 따라갈 이유가 없어진다.

## 사진은 서버로 보내지 않는다

아이 사진이다. 브라우저에서 `<canvas>`로 처리하고 **점 좌표와 번호만 남긴 뒤
사진은 버린다.** 서버로 가는 것은 좌표 배열뿐이다.

PDF를 서버에서 만드는 이유(4.4MB 한글 폰트, `docs/print-spec.md` §3)와 충돌하지
않는다. 점 100개의 좌표는 몇 킬로바이트고, 서버는 그것만 받아 지금과 같은 방식으로
PDF를 만든다.

**외부 라이브러리를 들이지 않는다.** OpenCV.js는 8MB가 넘어 이 사이트가 여는
첫 화면보다 크다. 아래 파이프라인은 전부 직접 구현할 수 있는 크기다.

## 윤곽선 따기

1. **줄인다** — 긴 변 1024px로. 노이즈가 줄고 뒤 단계가 전부 빨라진다.
2. **회색조 + 흐리기** — 가우시안. 옷의 무늬 같은 잔결을 없앤다.
3. **이진화** — Otsu 자동 임계값. 피사체와 배경을 가른다.
4. **윤곽 추적** — marching squares로 등고선을 딴다. 픽셀 경계를 그대로 따면
   계단이 생기므로 Chaikin으로 한 번 다듬는다.
5. **가장 큰 폐곡선 하나** — 면적 기준. 구멍(눈·입)은 1차에서 버린다(아래).
6. **단순화** — Ramer–Douglas–Peucker. 이때 남는 점이 곧 **형태의 모서리**다.

배경이 복잡하거나 대비가 낮은 사진은 여기서 실패한다. **실패를 조용히 넘기지
않는다** — 윤곽이 판 면적의 일정 비율에 못 미치거나 조각이 잘게 흩어지면 "이
사진은 배경이 단순한 것으로 바꾸거나 밝기를 조절해 보라"고 알린다. 임계값을
사용자가 미는 것은 `IDE-020`이다.

## 점 배분 — 균등하게만 찍으면 형태가 뭉개진다

호길이를 N등분해 찍으면 뾰족한 모서리가 두 점 사이에 묻힌다. 강아지 귀 끝이 점을
못 받으면 이어도 귀가 나오지 않는다.

- **모서리 먼저.** RDP가 남긴 점을 곡률이 큰 순으로 고른다.
- **나머지를 호길이 균등으로 채운다.** 긴 직선 구간이 점 없이 비지 않게.
- 모서리가 N보다 많으면 곡률 큰 것부터 N개, 적으면 모자란 만큼 균등 배분한다.

**입력한 개수를 지킨다.** 사용자가 30을 넣으면 정확히 30개다. 다만 상한이 있다 —
둘레를 최소 간격으로 나눈 수를 넘으면 번호가 서로 겹쳐 읽을 수 없다. 그때는 넣을
수 있는 최대치를 알려 주고 거기서 멈춘다(안내 문구는 `IDE-020`).

## 번호

- **점 위가 아니라 점 옆이다.** 숫자를 점에 겹치면 표적이 가려져 연필을 어디에
  댈지 알 수 없다. **윤곽선의 법선 방향 바깥쪽**에 놓아 도안 안쪽을 비운다 —
  아이가 그을 선이 지나갈 자리다.
- **1번은 가장 위쪽 점**, 진행은 시계 방향. 시작점에 화살표로 방향을 적는다.
- **닫힌 윤곽이라 마지막 점은 1번으로 돌아온다.** 마지막 번호 옆에 "1로"를 적지
  않으면 아이가 거기서 멈춘다.
- 번호끼리 겹치면 바깥쪽으로 더 밀어 피한다. 그래도 겹치면 점 개수가 상한을
  넘은 것이다.

## 실측으로 정할 값

종이에 뽑아 아이가 실제로 그어 봐야 정해지는 값들이다. 지금 적는 것은 출발점이다.

| 값             | 출발값   | 무엇으로 정하나                            |
| -------------- | -------- | ------------------------------------------ |
| 점 지름        | 1.8mm    | 연필 끝을 대기에 충분하고 선에 묻히지 않게 |
| 번호 글자 크기 | 3.2mm    | 만 4세가 혼자 읽는 최소 크기               |
| 최소 점 간격   | 8mm      | 점과 점 사이를 한 획으로 긋는 거리         |
| 안내선 농도    | 15% 회색 | 보고 따라 그을 수 있되 답이 되지는 않게    |

## 판

**190×277mm 세로 한 장**이다. A4 세로에서 인쇄 여백을 뺀 크기다.

**보드 파트는 정확히 1개**여야 하므로(`parseGame`) 가로 사진이 들어와도 이 판을
쓴다. 윤곽선을 판 안에 비율 그대로 맞춰 넣고 남는 쪽을 비운다. 가로 사진이면
위아래가 비는데, 사진을 돌려서 넣는 선택지를 `IDE-020`이 준다.

## 인쇄물 구성

- **점 잇기 판** (`board`, 190×277) — 점 · 번호 · 시작점 화살표 · 제목 칸
- **완성 그림** (`cutout`) — 다 이었을 때 나오는 실루엣. 어른이 먼저 보고 사진이
  제대로 땄는지 확인하는 용도다. 아이에게 먼저 보이면 놀이가 끝나므로 **따로
  뽑는 파트**로 뗀다.

## 슬롯

**점 개수는 지금 규격의 `number` 슬롯 그대로다.** 사용자가 요구한 "점의 갯수를
입력하면"이 여기 딱 맞는다 — 새로 만들 것이 없다.

| 슬롯         | 종류     | 값                                             |
| ------------ | -------- | ---------------------------------------------- |
| `title`      | `text`   | 도안 제목(예: "우리 강아지"). 판 위쪽에 찍는다 |
| `dot-count`  | `number` | 5~100, 정수. 기본 20                           |
| `guide-line` | `choice` | 안내선 없음 / 옅은 안내선. 기본 없음           |

`guide-line`은 나이를 가른다. 만 3~4세는 옅은 윤곽선이 있어야 선을 따라가고, 그
위 나이는 없어야 놀이가 된다.

## 사람 수와 준비물

혼자 하는 놀이라 `players`는 1~~1이다. 지금까지 네 게임이 전부 2인 이상이었으므로
**카탈로그가 "1~~1명"을 어떻게 보여 주는지** 확인해야 한다("1명"으로 읽혀야 한다).
준비물은 연필과 색연필이다.

## 안 하는 것

- **구멍과 조각.** 얼굴의 눈·입처럼 바깥 윤곽 안에 든 선은 1차에서 버린다. 선을
  떼었다 다시 시작하는 것은 이 놀이를 처음 하는 아이에게 어렵고, 도안에 "여기서
  연필을 떼라"를 표시하는 방법부터 정해야 한다. 실물로 놀아 본 뒤에 판단한다.
- **사진을 서버에 저장하는 것.** 다시 편집하려면 사진이 필요한데, 그 보관을
  어디에 할지는 `IDE-020`이 정한다.

## 할 일

- [x] `src/lib/dot-to-dot/` — 이진화 · 윤곽 추적 · 단순화 · 점 배분. 순수 함수로
      두고 DOM에 매지 않는다(테스트가 픽셀 배열을 직접 넣는다)
      → `image`(줄이기·회색조·Otsu) · `trace`(marching squares·Chaikin·RDP) ·
      `outline`(잇기·실패 판정) · `dots`(배분·번호) · `geometry`. 외부
      라이브러리 없이 다섯 파일, 브라우저·서버가 같은 코드를 쓴다
- [x] 실패 판정 — 윤곽이 너무 작거나 잘게 흩어졌을 때를 사유와 함께 돌려준다
      → 사유 넷(`empty` · `too-small` · `flooded` · `scattered`)과 한국어 한 줄.
      "비등한 덩어리 둘"과 "잘게 흩어진 배경"을 다른 잣대로 잡는다
- [x] 점·번호 SVG 생성기 — 우리가 쓰는 요소만 쓴다(`parseArtwork`가 읽는 범위)
      → `circle`·`text`·`path`·`line`뿐. `parseArtwork`가 모르는 요소를 쓰면
      예외로 터지므로 테스트가 두 파트를 다 파싱해 확인한다
- [x] 번호 겹침 회피와 점 개수 상한 계산
      → 상한은 `⌊둘레 ÷ 8mm⌋`. 겹침은 **바깥으로 밀고 안 되면 선을 따라 옆으로**
      비킨다 — 오목한 자리(귀 사이·목)에서는 바깥으로만 밀면 더 겹친다
- [x] 생성된 SVG를 미리보기·내보내기에 넣는 접점 — `LoadArtwork` 자리를 넓히되
      다른 게임의 정적 경로 규약은 그대로 둔다
      → **이미 `IDE-016` 2단계가 낸 자리(동적 파트)를 그대로 썼다.** 넓힌 것은
      `dynamic`의 `listSlotId`·`sizeSteps`를 선택으로 돌린 것뿐이다(크기 고정).
      정적 경로 규약은 한 줄도 안 고쳤다
- [x] 커스터마이즈 규격 확장 — 생성된 기하 데이터를 담는 `outline` 슬롯
      (원래 `IDE-020`의 할 일이었다. 아래 결정 기록)
- [x] 게임 정의 (`src/assets/games/dot-to-dot/`) · `registry.ts` 등록
      → 판(190×277)과 완성 그림(100×146) 두 파트, 슬롯 넷. 카탈로그·소개·만들기·
      인쇄 네 페이지가 다 200이다
- [x] `rules.ts` — 놀이 방법. 어른이 읽는 한 줄(사진 고르는 요령)도 넣는다
      → 「어른이 정할 것」 절에 나이별 점 개수 · 안내선 · `PHOTO_TIP`(배경이
      단순한 곳, 피사체가 화면 절반)이 들어갔다
- [x] 테스트 — 알려진 도형(원·별·사람 실루엣)을 넣어 점 개수가 맞는지, 모서리가
      점을 받는지, 번호가 겹치지 않는지
      → 53개. 별 꼭짓점 다섯이 3mm 안에 점을 받고, 상한까지 번호가 안 겹치며,
      사진 픽셀 → PDF까지 한 줄로 도는 것을 한 테스트가 확인한다
- [x] 앞선 네 게임의 산출 SVG가 바뀌지 않았는지
      → `npm run artwork` 전후 해시가 같다. 스키마를 고쳤는데도 네 게임의 도안
      정의는 한 줄도 안 바뀌었다
- [x] 작도 문서 `docs/dot-to-dot-artwork.md`
- [x] `docs/game-authoring.md` — "도안이 실행 시점에 생성되는 게임" 절을 더한다
      → 그 절과 「윤곽 슬롯」 절, 크기 고정 동적 파트를 함께 적었다

## 수용 기준

- [x] 사진 한 장과 점 개수를 넣으면 점·번호가 찍힌 A4 한 장이 나온다
      → 사람 실루엣 사진(픽셀)을 넣어 `POST /api/games/dot-to-dot/export`가
      36KB PDF 한 장을 냈다. 제목·점 30개·번호·시작 화살표가 다 있다
- [x] 넣은 개수와 찍힌 점의 개수가 같다(상한에 걸리면 그 사실을 알린다)
      → 5·10·20·24·50·60개를 넣어 원과 숫자 수가 같다. 넘으면 `clamped`와
      `capacity`를 함께 돌려준다
- [x] 이은 결과가 원본 사진의 실루엣으로 읽힌다 — 모서리가 뭉개지지 않는다
      → 별 꼭짓점 다섯이 모두 3mm 안에 점을 받는다. 사람 실루엣의 오목한
      자리(겨드랑이)도 살아남는다. ⚠︎ **사람 눈으로 읽히는지는 「놀아 보기」**
- [x] 번호가 서로 겹치지 않고, 점을 가리지 않는다
      → 보기 그림에서 상한(86개)까지 겹침 0. 번호는 늘 윤곽선 바깥쪽이다
- [x] 배경이 복잡한 사진에서 빈 판이 조용히 나오지 않고 사유가 나온다
      → `scattered`와 "배경이 단순한 곳에서 찍은 사진이 잘 됩니다". 이 사유를
      화면 문구로 옮기는 것은 `IDE-020`이다
- [x] 사진이 서버로 가지 않는다 — 내보내기 요청 본문에 이미지가 없다
      → 점 30개짜리 본문이 1,171바이트다. 요청 규격이 글자·수·좌표만 받아
      데이터 URL이 낄 자리가 없고, 테스트가 그것을 확인한다
- [x] 앞선 네 게임의 산출 SVG가 바뀌지 않았다
- [ ] 종이 실측 — 100%에서 점이 연필 끝을 대기에 알맞고, 번호를 아이가 읽는다
- [ ] 놀아 보기 — 만 4세가 20개, 만 6세가 50개를 혼자 끝까지 잇는가

## 결정 기록

- **2026-09-08 — 사용자가 점 잇기를 요청했다.** 사진 입력 · 점 개수 입력 · 점 위
  번호 세 가지를 지정했고, 목적이 숫자 읽기와 운필력이라고 밝혔다.
- **2026-09-08 — 번호는 점 위가 아니라 점 옆에 둔다.** 요청 문구는 "점 위에
  숫자"였지만 겹쳐 찍으면 연필을 댈 표적이 가려진다. 윤곽선 바깥쪽으로 민다.
  사용자가 뒤집으면 그때 고친다.
- **2026-09-08 — 사진은 브라우저에서만 다룬다.** 아이 사진이 서버에 남지 않게
  하는 것이 먼저다. 좌표만 보내면 서버 PDF 생성과도 충돌하지 않는다.
- **2026-09-08 — 기본값을 정해 두고 `todo`로 시작한다.** 판 크기(190×277) · 점
  개수 범위(5~100) · 실측 출발값은 이 저장소의 기존 치수와 인쇄 여백에서 끌어온
  것이라 답을 기다리지 않는다.
- **2026-09-09 — 접점은 새로 내지 않았다. `IDE-016`의 동적 파트를 그대로 썼다.**
  이 이슈가 "가장 되돌리기 비싼 결정"으로 꼽은 자리인데, 그 사이에 `IDE-016`
  2단계가 같은 문제를 이미 풀어 두었다 — 파트에 `dynamic`이 있으면 미리보기는
  `POST /api/games/<id>/artwork`로, 내보내기는 `composeExport`의 `renderArtwork`
  로 간다. **게임 정의가 SVG를 들고 다니지 않는다**는 조건이 거기서 이미
  지켜진다. 넓힌 것은 하나뿐이다: `dynamic`의 `listSlotId`·`sizeSteps`를 선택
  으로 돌려 **크기가 고정인 동적 파트**를 허용했다. 사진이 무엇이든 점 잇기 판은
  190×277 한 장이고 바뀌는 것은 그 위의 점과 번호뿐이다. 다른 네 게임의 도안
  정의는 한 줄도 안 고쳤다.
- **2026-09-09 — 생성된 기하 데이터를 담는 `outline` 슬롯을 여기서 만들었다.**
  원래 `IDE-020`의 할 일이었지만, 그것 없이는 이 이슈의 수용 기준 첫 줄("사진
  한 장과 점 개수를 넣으면 …")이 성립하지 않는다 — 값을 담을 자리가 없으면
  서버가 그릴 것도 없다. `IDE-020`이 적어 둔 "먼저 하는 쪽에 맞춘다"에 따라
  이쪽이 정했다. 값은 `[x0, y0, x1, y1, …]` 납작한 mm 좌표이고 배치는 `control`
  뿐이다. `list` 슬롯으로 대신하지 않은 이유는 그쪽이 여전히 **고르는 것**
  (`options`를 미리 선언한다)이고 이쪽은 **계산되는 것**이기 때문이다.
- **2026-09-09 — 슬롯 최대치 100은 대체로 볼록한 피사체에서는 다 안 들어간다.**
  상한이 `⌊둘레 ÷ 8mm⌋`인데, 판 안쪽 상자(144×208)에 들어가는 볼록한 도형의
  둘레가 최대 704mm라 88개쯤에서 걸린다(보기 그림은 86). 만 6세 목표인 50은
  넉넉하다. 범위를 줄이지 않은 것은 **오목한 사진에서는 100이 실제로 들어가고**,
  못 들어갈 때 조용히 줄이지 않고 최대치를 함께 알리기 때문이다. 최소 간격 8mm는
  종이 실측으로 정할 값이라 6mm가 되면 상한이 118로 오른다.
- **2026-09-09 — 번호 겹침은 바깥으로만 밀어서는 안 풀린다.** 처음엔 법선 방향
  으로 밀기만 했는데 상한 근처에서 겹쳤다. **오목한 자리**(고양이의 두 귀 사이,
  목)에서는 바깥 법선이 한 점으로 모여 밀수록 더 겹치기 때문이다. 후보를 바깥
  거리 × 진행 방향 거리의 격자로 두고 제 점에서 가까운 것부터 쓰게 고쳤다.
- **2026-09-09 — 기본값을 보기 그림(앉은 고양이)으로 뒀다.** 사진 넣기는
  `IDE-020`이라 지금 만들기 화면에서 고칠 수 있는 것은 제목·점 개수·안내선
  셋이고 윤곽은 이 그림에 머문다. 빈 판을 기본값으로 두면 카탈로그 썸네일과 소개
  페이지에 아무것도 없는 종이가 걸린다. 몸은 반쪽만 적어 뒤집고 **꼬리만
  뒤집지 않는다** — 처음에 대칭에 함께 태웠더니 꼬리가 양쪽에 달려 고양이가
  아니라 날개 달린 무엇이 됐다.
- **2026-09-09 — 카탈로그가 `1~1`을 "1인용"으로 읽는다.** 확인만 하면 되는
  것이었다 — `formatPlayers`가 최소·최대가 같으면 한 번만 적는다. 고칠 코드가
  없었고 테스트만 더했다.
