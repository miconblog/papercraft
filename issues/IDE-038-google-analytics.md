# IDE-038 · Google Analytics 4 — 자체 통계와 병행 · 개인정보처리방침

| 항목        | 값        |
| ----------- | --------- |
| 상태        | `review`  |
| 영역        | frontend  |
| 우선순위    | P2        |
| 마일스톤    | M3        |
| 추정        | 0.5d      |
| 추천 모델   | Sonnet 5  |
| 추천 Effort | medium    |
| 선행        | `IDE-013` |
| 후행        | —         |

## 배경

사용자 요청(2026-09-19) — "GA 를 추가하고 싶어."

`IDE-013` 은 **외부 분석 도구를 붙이지 않는다**로 시작했다 — 아이와 함께 쓰는
사이트라 방문 기록을 제3자에게 넘기지 않는 편이 낫고, 필요한 지표가 좁았다.
이번 요청은 그 결정을 되돌리는 것이라 범위를 먼저 물었고, 사용자는 **자체 통계와
병행**을 골랐다(유입 · 퍼널 · 지도는 그대로, 검색 유입 · 서치 콘솔 연동처럼 GA 에만
있는 것을 얻는다).

고지를 두고 사용자가 "블로그는 보통 안 하던데"라고 물었다. 플랫폼 블로그는
플랫폼이 처리방침을 대신 게시하지만, 직접 운영하는 사이트에 GA 를 붙이면 그 몫이
주인에게 온다 — **GA 이용약관이 처리방침에 GA 사용을 적도록 요구하고**,
개인정보보호법도 쿠키 같은 자동 수집 장치와 거부 방법을 적게 한다. 그래서 동의
배너 없이 **고지만** 하기로 했다.

## 할 일

- [x] `lib/analytics/google.ts` — 측정 ID(`G-7VJZZEC422`, 2026-09-19 사용자),
      싣는 조건, 관리자 판정, 초기화 문자열
- [x] **운영 배포에서만** 싣는다(`VERCEL_ENV=production`).
      `NEXT_PUBLIC_GA_ID` 가 있으면 그것(`off` 면 끈다)
- [x] 관리자 제외 — `dc_owner` 쿠키 · `/admin` 경로(자체 통계와 같은 목록
      `EXCLUDED_PREFIXES` 를 나눠 쓴다). GA 의 공식 끄기 스위치
      `window['ga-disable-<ID>']` 를 `config` **전에** 세우고, 화면이 바뀔 때마다
      다시 세운다
- [x] 광고 신호 끄기 — `allow_google_signals` · `allow_ad_personalization_signals`
- [x] `/privacy` — 자체 통계 · GA(쿠키 · 거부 방법) · 댓글 · 브라우저에만 남는 것 ·
      연락처. 하단 줄에 링크
- [x] 테스트 13개 — 싣는 조건 · 관리자 판정 · **초기화 문자열을 실제로 돌려
      TypeScript 판정과 같은 답인지** · 광고 신호 · 화면 전환 · 처리방침
- [x] 빌드 확인 — `VERCEL_ENV=production` 빌드의 HTML 에 gtag 가 실리고, 조건 없는
      빌드에는 측정 ID 가 0건
- [ ] ⚠︎ 배포 뒤 GA 실시간 보고서에 내 방문이 **안** 잡히는지(관리자 브라우저),
      시크릿 창 방문은 잡히는지
- [ ] GA 관리 화면 — 데이터 보관 기간 · 서치 콘솔 연결(사용자)

## 수용 기준

- [x] 로컬 · CI · 미리보기 배포에는 GA 스크립트가 없다
- [x] 관리자 브라우저와 `/admin` 은 GA 로 아무것도 보내지 않는다(스위치로)
- [x] 광고 신호 둘이 꺼진 채로 설정된다
- [x] 모든 화면의 하단에서 처리방침에 닿고, 처리방침에 GA 사용 · 쿠키 · 거부하는 법 ·
      연락처가 있다
- [ ] ⚠︎ 실제 GA 속성에 공개 화면의 페이지뷰가 들어온다

## 결정 기록

- **2026-09-19 — `IDE-013` 의 "외부 도구 안 붙임"을 병행으로 바꿨다**(사용자 결정).
  자체 통계는 그대로 둔다 — IP 원본을 남기지 않는 쪽이 여전히 주 통계다.
- **2026-09-19 — 동의 배너 없이 고지만**(사용자 결정). 대신 광고 신호를 꺼서 고지로
  충분한 범위에 머문다. EU 방문자까지 엄밀히 따지면 동의가 필요하지만, 이 사이트의
  방문은 거의 한국이다(`IDE-033` 나라 지도).
- **2026-09-19 — `@next/third-parties` 의 `GoogleAnalytics` 를 쓰지 않았다.** 설정
  값(광고 신호)과 `config` 전에 세울 끄기 스위치를 넣을 자리가 없다. `next/script`
  둘(초기화 · gtag)로 직접 싣는다.
- **2026-09-19 — 페이지뷰는 GA 가 스스로 보낸다**(향상된 측정의 브라우저 기록 기반
  페이지 변경). 우리가 따로 보내면 두 번 센다. 관리자 제외는 스위치가 맡는다.
  - 남는 틈: 관리자가 **처음 로그인하는 순간**에는 쿠키가 생긴 뒤 스위치가 서기
    전에 한 번 셀 수 있다. 로그아웃해도 `dc_owner` 가 남으므로(`IDE-026`) 평생
    한 번이다.
- **2026-09-19 — 측정 ID 를 코드에 적었다.** 페이지 소스에 그대로 실리는 공개
  값이고, 운영 배포에서만 싣는 조건이 따로 있어 환경변수를 넣지 않아도 된다.
- **2026-09-19 — 처리방침 연락처는 예전 하단 줄에 공개돼 있던
  `miconblog@gmail.com`** 이다(사용자 확인 필요).
