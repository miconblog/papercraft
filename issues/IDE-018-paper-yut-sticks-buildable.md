# IDE-018 · 종이 윷가락 — 던져서 구르는 조립 도안

| 항목        | 값          |
| ----------- | ----------- |
| 상태        | `review`    |
| 영역        | content     |
| 우선순위    | P1          |
| 마일스톤    | M2          |
| 추정        | 3d          |
| 추천 모델   | Opus 5      |
| 추천 Effort | xhigh       |
| 선행        | ~~IDE-017~~ |
| 후행        | —           |

## 배경

사용자가 윷놀이를 고르며 **"종이 윷가락을 입체로 만드는 도안"** 을 함께
요구했다(2026-09-08). 말판은 `IDE-017`이고, 이 이슈는 그 판을 굴리는 물건이다.

`IDE-017`에서 뗀 이유는 확인할 것이 다르기 때문이다. 말판은 **인쇄물**이라 종이에
대고 재면 끝나지만, 윷가락은 **던져서 굴러야 하는 물건**이다. 눈이 나오는 비율이
한쪽으로 쏠리거나, 가벼워서 튀거나, 모서리로 서 버리면 도안이 틀린 것인데 이건
그려 놓고는 알 수 없고 **실물을 접어 던져 봐야** 안다.

이 저장소에 이미 조립물이 하나 있다 — 축구 골대(`IDE-004`,
`src/assets/games/soccer/artwork/goals.ts`). **풀도 칼도 쓰지 않고** 탭·귀·겹이
사슬처럼 물리게 짠 것이고, 커밋 셋을 먹으며 사용자가 손으로 접어 본 지적으로
세 번 다시 그려졌다. 윷가락도 같은 길을 갈 것으로 본다.

## 무엇을 만드는가

**윷가락 넷.** 실제 윷은 통나무를 반으로 쪼갠 **반원기둥**이라 평평한 면(배)과
둥근 면(등)이 있고, 넷을 던져 배가 몇이냐로 도·개·걸·윷·모가 갈린다.

종이로는 곡면을 감기 어렵고 감아도 눌린다. 그래서 **정육각형을 반으로 자른 단면**
으로 근사한다 — 평평한 배 한 면과 등 세 면이고, 접는 선이 전부 직선이라 자와
손톱만으로 접힌다. 반원의 근사로 충분하고, 등이 세 면이라 굴러가다 멈추는
자리가 실물과 비슷하다.

```
   ___________          단면: 정육각형의 아래 절반
  /           \         · 배(밑면) 폭 W
 /             \        · 등 세 면, 각 W/2
/_______________\       · 높이 (√3/4)·W ≈ 0.43W
      (배)
```

전개도 한 가락은 **직사각 넷 + 양 끝 마구리 + 잠금**이다. 폭은 `W + 3·(W/2)`,
여기에 마구리와 잠금이 붙는다. `W = 16mm`, 길이 `110mm`면 전개도 폭이 40mm 남짓
이라 **A4 세로 한 장에 넉 장이 나란히** 들어간다.

**속을 비워 두면 안 된다.** 종이 한 겹짜리 관은 너무 가벼워 던지면 튀고, 손으로
쥐면 찌그러진다. 안으로 접어 넣는 겹을 두어 두께와 무게를 준다 — 골대 뚜껑을
반으로 접어 두 겹으로 만든 것과 같은 수법이고(2026-09-08 사용자 지적 "뚜껑이 한
겹이라 약해"), 여기서는 강성뿐 아니라 **무게중심**이 걸려 있다.

**백도 가락 하나는 다르게 인쇄한다.** 넷 중 하나의 배 면에 표식이 들어가고,
`IDE-017`의 규칙이 그 표식을 읽는다.

## 던져 보기 전에는 모르는 것들

이 이슈의 수용 기준이 대부분 여기 걸린다.

- **배가 나오는 비율.** 이론상 절반이지만 종이 윷은 배 면이 가장 넓어 배로 누우려
  한다. 한쪽으로 쏠리면 도·개가 지나치게 잦고 모가 안 나와 놀이가 늘어진다.
- **모서리로 서는 것.** 등 세 면의 경계에 걸려 서면 눈을 읽을 수 없다. 단면 높이
  대 길이 비를 조절해 없앤다.
- **튐.** 가벼우면 굴러가 상 밖으로 나간다. 겹으로 무게를 주고, 그래도 모자라면
  길이를 줄인다.
- **벌어짐.** 던지는 물건이라 잠금이 헐거우면 몇 판 만에 풀린다. 골대처럼 풀 없이
  물리는 것을 먼저 시도하고, 안 되면 풀칠면(`glue`)을 낸다 — 다른 조립물이 풀을
  안 쓰므로 여기서 처음 쓰게 되면 그 이유를 문서에 남긴다.

## 할 일

- [x] `dimensions.ts` — 단면 폭·길이·겹 깊이·잠금 치수. 왜 그 값인지 골대
      (`GOAL` 주석)처럼 근거를 함께 적는다
      — 배 16 · 등 8씩 · 길이 110 · 갈고리 5 · 마구리 여유 0.4 · 탭 6×7.
      **`bellyMm`이 조절 나사 하나**라는 것을 주석과 문서에 적었고, 높이·기울기·
      마구리는 거기서 계산해 뽑는다
- [x] 윷가락 전개도 SVG (`buildable`, A4 세로 한 장에 넉 장) — 오림선 ·
      접는선(산·골) · 겹 · 잠금
      — `artwork/sticks.ts`. **나란히가 아니라 눕혀 넉 줄**이다(속대가 붙어
      전개도 폭이 40 → 61mm라 세워 늘어놓으면 244mm로 A4를 넘는다). 남는 오른쪽
      단에 도해 넉 컷이 들어갔다. 시트 194×281 — 여백 8mm까지 한 장
- [x] 백도 가락 — 넷 중 하나의 배 면에 표식
      — **과녁(겹동그라미)**이다. 던진 가락은 좌우가 뒤집혀 떨어지므로 방향이
      있는 그림은 절반이 거꾸로 읽힌다. 글자는 양 끝에 하나씩(한쪽은 180°)
- [x] 배·등 인쇄 — 어느 면이 배인지 접기 전에 알아볼 수 있게 한다
      — **등 세 면에만 나뭇결, 배는 민짜.** 쪼갠 통나무의 겉과 쪼갠 자리를 옮긴
      대비라 접기 전에도 접고 나서도 같은 규칙으로 읽힌다
- [x] 조립 순서 안내 — 전개도 여백에 번호로. 골대가 탭·귀·겹을 번호로 부르는
      것과 같은 방식이다
      — `1 속대 · 2 마구리 · 3 탭`. 시트의 번호와 규칙문의 "1번 속대"가 같은
      말을 쓰는지 테스트가 본다. 도해 ①~④가 오른쪽 단에 있다
- [x] 도안 테스트 — 접는선이 오림선 안에 있는가, 넉 장이 서로 겹치지 않는가,
      전개도가 A4에 들어가는가
      — 셋 다 넣었고(접는선은 광선 쏘기로 윤곽 안인지 본다), 그 밖에 단면이
      정육각형 절반인가 · 속대가 무게중심을 올렸는가 · 빗면이 마루로 넘어가는가 ·
      배 위 몫의 구간이 0.5를 품는가까지 본다
- [x] `IDE-017`의 게임에 파트로 등록
      — `sticks` 파트(`buildable`). **스키마는 한 줄도 안 고쳤다.** 한 게임에
      조립물이 둘인 첫 게임이다
- [x] 작도 문서 — `docs/yut-nori-artwork.md`에 절을 더한다
      — §7. 게임 방법 부속(§6)의 높이 240 → 266도 같이 고쳤다

## 수용 기준

- [ ] 100%로 뽑아 **가위와 접기만으로** 넉 장이 조립된다 (풀을 쓰기로 했다면
      풀칠면이 도안에 있고 이유가 문서에 있다)
      — ⚠︎ **풀칠면을 내지 않았다**(`glue` 층이 없다는 것을 테스트가 지킨다).
      실제로 가위와 접기만으로 되는지는 접어 봐야 한다
- [x] 커밋된 SVG가 생성기와 같다 (`npm run test:run`)
      — `yut-nori/__tests__/artwork.test.ts` 44개 통과. (같은 실행에서
      `dot-to-dot` 하나가 깨지는데 `IDE-019`가 작업 중인 파일이고 이 이슈와
      무관하다)
- [ ] **100번 던져** 배가 나온 비율이 40~60% 안이다
      — ⚠︎ 종이 위 정적 모형으로는 **0.28~0.63 구간**까지만 좁혔다(빗면에 얹힌
      것이 얼마나 마루로 넘어오느냐가 갈린다). 벗어나면 `bellyMm` 하나를 고친다
- [ ] **100번 던져** 모서리로 서서 눈을 읽을 수 없는 경우가 없다
      — ⚠︎ 모형상 빗면의 무게중심이 마루 쪽 끝에서 0.9mm라 넘어간다. 실물 대기
- [ ] 상 위에서 던져 밖으로 나가지 않는다
      — ⚠︎ 규칙문에 "담요나 방석 위에 던진다"를 넣었다. 그래도 나가면 배율을
      키우거나 길이를 줄인다. 실물 대기
- [ ] 한 판(대략 30~50번 던지기)을 놀고도 가락이 벌어지거나 찌그러지지 않는다
      — ⚠︎ 실물 대기. 잠금이 셋(속대·마구리·탭)이라 하나가 헐거워도 나머지가
      잡게 짰다
- [x] 백도 가락이 나머지 셋과 구별되고, 흑백으로 뽑아도 구별된다
      — 과녁과 글자 모두 **잉크만** 쓴다(색을 쓰지 않는 것을 테스트가 지킨다).
      나머지 셋의 배는 민짜라 한 장만 다르다

## 결정 기록

- **2026-09-08 — 반원기둥이 아니라 반육각기둥으로 근사한다.** 종이로 곡면을 감으면
  눌리고, 접는 선이 전부 직선이어야 자 없이 접힌다. 실물과 다른 것은 등이 세
  면으로 각졌다는 것뿐인데, 그 각이 오히려 굴러 멈추는 자리를 안정시킨다.

- **2026-09-09 — 선행 `IDE-017`이 `done`이 아닌 채로 착수했다.** 아직 `review`
  이지만 남은 것이 사용자의 종이 실측뿐이고 게임 정의·아트워크는 이미 커밋돼
  있어(46a9ad2) 파트를 붙일 자리가 다 있었다. 이제 안 막으므로 선행에 취소선을
  그었다.

- **2026-09-09 — 전개도를 띠 일곱이 한 방향으로 감기는 나선으로 짰다.**
  `등1 → 등2(마루) → 등3 → 배`가 관 바깥이고, 배에서 모서리를 한 번 더 돌면
  종이가 **그대로 안으로 들어가** 속대가 된다. 접는 방향이 여섯 줄 모두 같아
  "전부 같은 쪽으로 만다" 한 줄로 설명이 끝난다 — 골대가 "전부 골접기"였던 것과
  같은 판단이다. 이음매는 배–등1 모서리이고 속대가 그 밑을 가로지른다.

- **2026-09-09 — 겹(속대)을 배가 아니라 등 쪽에 넣었다. 이 이슈에서 가장 되돌리기
  어려운 결정이다.** 이슈가 "겹은 강성뿐 아니라 무게중심이 걸려 있다"고 적어 둔
  자리인데, 따져 보니 **방향이 정해져 있었다** — 배는 이 단면에서 가장 넓어
  그러잖아도 배를 깔고 눕기 쉬운데, 겹까지 배에 넣으면 무게중심이 내려가 더
  눕는다. 등 쪽에 넣으면 무게중심이 배에서 2.77 → **3.57mm**로 올라가 (단면 높이
  6.93의 0.40 → 0.52) 배가 위로 오는 쪽이 그만큼 는다. 그 값을 `stickBalance()`가
  세고 도안 테스트가 지킨다.

- **2026-09-09 — 던져 보기 전에 알 수 있는 데까지를 코드로 옮겼다.**
  수용 기준 넷이 실물에 걸려 있어 도안만으로는 아무것도 확인되지 않는 이슈였다.
  그래서 무게중심과 **면이 차지하는 각도**(주사위의 공평함을 재는 어림)를 계산해,
  배가 위로 올 몫을 **구간**으로 낸다 — 빗면에 얹힌 것이 하나도 안 넘어오면 0.28,
  다 넘어오면 0.63이다. 이 구간이 0.5를 품지 못하면 던져 보나 마나 쏠린 단면이고,
  그것을 테스트가 잡는다. **던져 보는 것을 대신하지 않는다**는 것을 코드 주석과
  문서 양쪽에 못 박았다.

- **2026-09-09 — 잠금은 셋이고 풀을 쓰지 않는다.** `1 속대`가 이음매를 안에서
  받치고 갈고리로 모서리를 하나 더 넘어 스스로 갇힌다. `2 마구리`가 관 끝에서
  단면을 붙들고, 그 `3 탭`이 **속대 밑으로** 들어가 마구리를 붙든다. 골대의
  탭·귀·겹과 같은 사슬이고 번호를 부르는 방식도 같다.
  `IDE-015`의 종이 주사위가 이미 풀을 쓰므로 "다른 조립물이 풀을 안 쓴다"는 배경의
  전제는 이미 어긋나 있었지만, 어차피 필요가 없어 쓰지 않았다.

- **2026-09-09 — 골대와 달리 홈을 파지 않는다.** 마구리가 배 띠의 끝에만 붙어
  이웃한 등1·등3의 끝과 **꼭짓점 하나로만** 만나기 때문이다. 골대는 뚜껑 귀·모서리
  탭·옆벽이 전개도에서 위아래로 맞닿아 홈이 넷 필요했다. 띠 블록 밖 윤곽점의 y가
  모두 배 띠 안쪽인지를 테스트가 확인한다.

- **2026-09-09 — 넉 장을 나란히가 아니라 눕혀 넉 줄로 앉혔다.** 이슈는 "전개도 폭이
  40mm 남짓이라 A4 세로 한 장에 넉 장이 나란히"로 잡았는데, 속대가 붙으면서 폭이
  **61mm**가 됐다 — 세워 늘어놓으면 244mm로 A4 폭을 넘는다. 눕히면 한 벌이 110mm
  뿐이라 오른쪽에 도해를 놓을 단이 남는다. 시트는 **194×281**로, 골대 시트와 같은
  기준(인쇄 여백 8mm까지 한 장)을 지킨다 — 조립물이 두 장으로 쪼개지면 이어 붙인
  자리에서 전개도가 어긋나기 때문이다.

- **2026-09-09 — 배는 민짜, 등에는 나뭇결.** "어느 면이 배인지 접기 전에 알아볼
  수 있게"의 답을 글자가 아니라 **면의 질감**으로 냈다. 쪼갠 통나무의 겉이 등이고
  쪼갠 자리가 배라는 실물의 대비 그대로라, 접기 전(띠 고르기)과 접은 뒤(눈 읽기)에
  같은 규칙이 쓰인다. 결은 끊어 그린다 — 이으면 괘선이 되어 접는선과 헷갈린다.

- **2026-09-09 — 백도 표식은 과녁이고 색을 쓰지 않는다.** 던진 가락은 좌우가
  뒤집혀 떨어지므로 방향이 있는 그림은 절반이 거꾸로 읽힌다. 겹동그라미는 어느
  쪽으로 누워도 같은 모양이다. 색을 안 쓰는 것은 흑백 출력 규약(`IDE-009`)이고,
  나머지 셋의 배가 민짜라 한 장만 달라 보인다.

- **2026-09-09 — 게임 방법 부속을 240 → 266mm로 키웠다.** 윷가락 접는 법 네 줄이
  들어오면서 240에서는 둘째 단이 넘쳤다. "넘치면 글자가 아니라 글을 줄인다"는
  규약은 그대로지만 **조립 순서는 한 줄만 빠져도 못 접는다.** 폭 190은 그대로라
  인쇄 여백 10mm까지 A4 한 장인 것도 그대로다.

- **2026-09-09 — 배율을 0.9~1.3으로 좁혔다.** 다른 부속과 달리 **던져서 구르는
  물건이라 배율이 곧 놀이의 성질**이다. 100%가 원칙이고, 종이가 얇아 자꾸 튀면
  조금 키워 무게를 벌라고 위쪽만 열었다.

- **2026-09-09 — 남은 것은 실물이다.** 접어서 100번 던져 봐야 정해지는 것이 넷이다
  (배 비율 · 모서리로 서기 · 튐 · 벌어짐). 벗어났을 때 고칠 나사는 문서에 적어
  두었다 — 비율은 `bellyMm` 하나, 튐은 던지는 자리(담요)와 배율이다.
