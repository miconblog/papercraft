# IDE-006 · 커스터마이즈 에디터

| 항목        | 값                                  |
| ----------- | ----------------------------------- |
| 상태 | `done` |
| 영역        | frontend                            |
| 우선순위    | P0                                  |
| 마일스톤    | M1                                  |
| 추정        | 5d                                  |
| 추천 모델   | Sonnet 5                            |
| 추천 Effort | high                                |
| 선행        | ~~IDE-003~~ · `IDE-004` · ~~IDE-010~~ |
| 후행        | `IDE-007` · `IDE-009`               |

## 배경

사이트의 핵심 상호작용이다. 방문자가 도안의 정해진 슬롯 — 축구 게임판이면 선수
등번호, 팀명, 팀 색 — 을 채우고 결과를 화면에서 바로 확인한다.

슬롯 타입은 `IDE-003`에서 정의되므로 에디터는 게임마다 새로 만들지 않는다. 스키마를
읽어 폼을 생성하는 방식이어야 게임이 늘어도 에디터는 하나로 유지된다.

## 할 일

- [x] 슬롯 타입별 입력 컴포넌트 구현 (텍스트 · 숫자 · 색상 · 선택)
  - `SlotField`가 `kind`로 네 입력을 고른다. 색상은 `<input type=color">` +
    `palette`가 있으면 스와치 버튼도 같이 낸다. **선택(`choice`)은 사용자
    요청으로 네이티브 `<select>` 대신 shadcn/ui `Select`로 교체했다** —
    아래 결정 기록 참고
- [x] 스키마를 읽어 폼을 자동 생성하는 렌더러
  - `CustomizationForm`이 `game.groups`·`game.slots`만 순회한다. 그룹 소속
    슬롯은 그룹 섹션(이름·색 먼저, 마커는 격자로) · 그룹 없는 슬롯은 별도
    섹션 — 축구 게임판이라면 `marker-style` 선택 슬롯이 후자다
- [x] 도안 미리보기 — 입력이 바뀌면 즉시 반영
  - `BoardPreview`가 파트 SVG를 배경으로 fetch하고(팀 색은 레이어 fill 치환),
    텍스트·마커 슬롯은 같은 viewBox의 오버레이 `<svg>`로 그 위에 겹친다.
    실제 브라우저(playwright)로 등번호·팀명·팀색 변경이 미리보기에 그대로
    반영되는 것을 확인했다. **마커 스타일(원형·일러스트)도 사용자 지적으로
    실제 아트워크를 fetch해 팀 색으로 칠해 그린다** — 아래 결정 기록 참고
- [x] 미리보기의 슬롯을 눌러 해당 입력으로 이동
  - 오버레이의 텍스트·마커를 누르면 `slot-field-<슬롯id>` id를 가진 입력으로
    스크롤 + 포커스. playwright로 실제 클릭 → 포커스 이동을 확인했다
- [x] 입력 검증과 오류 표시 (글자 수 초과, 범위 밖 숫자)
  - 슬롯 값이 바뀔 때마다 `validateSlotValue`(IDE-003이 이미 제공)를 돌려
    `SlotField` 아래에 `role="alert"`로 사유를 보여준다
- [x] 초기화 · 기본값 되돌리기
  - "기본값으로 되돌리기" 버튼이 `defaultCustomization(game)`으로 되돌린다
- [x] 새로고침에도 입력이 남도록 로컬 저장
  - `localStorage`에 게임별 키로 저장(`saveCustomization`)하고, 하이드레이션
    이후 `useStoredCustomization`으로 복원한다. 저장값이 지금 도안과 안
    맞으면(`validateCustomization` 실패) 조용히 버리고 기본값으로 시작한다
- [x] 전술 대형(배치 프리셋) 적용 UI — 그룹별로 고를 수 있는 대형 버튼
  - review 후 사용자 요청으로 추가했다. `FormationPicker`가 그룹의
    `game.presets`만 읽어 버튼을 늘어놓고, 누르면 `applyPreset`으로 그
    그룹 마커의 좌표만 옮긴다(다른 그룹·값은 그대로). 되돌리기를 누르면
    선택 표시도 함께 풀린다 — 아래 결정 기록 참고

## 수용 기준

- [x] 축구 게임판의 등번호 22개, 팀명 2개, 팀 색 2개를 모두 편집할 수 있다 — `EditorClient` 테스트가 실제 축구 게임 도안으로 개수를 센다
- [x] 입력 변경이 미리보기에 반영된다
- [x] 새로고침해도 입력값이 유지된다 — 저장 후 재마운트해도 복원되는 것을 테스트와 실제 브라우저 양쪽으로 확인
- [x] 에디터 코드에 특정 게임 이름이 하드코딩되어 있지 않다 — `CustomizationForm`·`BoardPreview` 모두 `GameDefinition`만 읽는다. 테스트도 최소 픽스처(`makeGameWithMarkers`)로 검증한다

## 결정 기록

- 2026-09-05 · **선행 `IDE-004`가 아직 `blocked`인 채로 착수했다.** 남은 항목은
  종이 실측과 파울·아웃 규칙 확인뿐이고, 에디터가 실제로 읽는 슬롯·파트 스키마는
  이미 확정돼 있었다(`IDE-004`의 해당 할 일은 `[x]`). `BOARD.md`도 착수
  가능하다고 명시해 실질적 장애가 없다고 보고 진행했다.
- ~~2026-09-05 · **마커 미리보기를 원 + 등번호로 단순화했다.** 실제 마커
  아트워크(원형·일러스트 두 벌, `IDE-010`)를 오버레이에 그대로 쓰려면 슬롯마다
  변형 SVG를 추가로 fetch해야 해 범위가 커진다. 수용 기준은 "입력이 미리보기에
  반영된다"이지 "실제 아트워크가 그대로 보인다"가 아니라, 팀 색을 채운 원 +
  값 텍스트로 낮췄다. 실제 마커 그림을 미리보기에 그대로 쓰는 것은 필요해지면
  별도 이슈로 뺀다.~~ → **사용자가 "선수 일러스트를 선택해도 사람 모양이 안
  보인다"고 지적해 뒤집었다**(같은 날). 아래 결정 기록 참고.
- 2026-09-05 · **마커 스타일 변형의 실제 아트워크를 fetch해 오버레이에
  그린다.** `game.styleSets`의 모든 변형(`variant.artwork`)을 마운트 시 한
  번에 fetch하고, 선택된 변형의 SVG를 `pc-marker-fill` 레이어(색을 받는
  관례 — `player-markers.ts`가 정하고 "렌더러가 채워 넣는다"고 문서화해 둔
  것)에 팀 색을 입힌 뒤 `extractSvgInner`로 바깥 `<svg>` 태그만 벗겨
  `<g transform="translate(...)">` 안에 `dangerouslySetInnerHTML`로 끼워
  넣는다. 기준점이 마커 중심이라(`IDE-003`) `translate`만으로 좌표가
  맞는다. 아트워크를 아직 못 불러왔거나 없으면 이전의 원 + 값 텍스트로
  대체한다(로딩 중 깜빡임 대신 즉시 뭔가는 보이게).
  - **원정 팀 마커의 좌우 반전(`scale(-1,1)`)은 이번에도 넣지 않았다.**
    `player-markers.ts`는 "렌더러가 원정 마커에 반전을 건다"고 문서화해
    뒀지만, 어느 그룹이 "원정"인지 판단할 스키마 필드가 없다(그룹에
    "진영 방향" 개념이 없다) — 게임 이름을 하드코딩하지 않고는 판단할
    길이 없어 미뤘다. 지금은 양 팀이 같은 방향을 보는 그림으로 나간다.
    화살촉 방향과 정확한 진영 표시가 필요해지면 스키마에 필드를 먼저
    추가해야 한다.
  - 등번호 텍스트는 여전히 마커 중심에 그린다 — 원형·일러스트 둘 다
    몸통·원의 중심이 마커 중심 근처라 겹쳐도 읽힌다. 실제 브라우저로
    확대해 확인했다(작은 전체 화면 스크린샷에서는 숫자가 안 보일 만큼
    작아 처음엔 나도 놓쳤다 — 실제로는 정상 렌더링되고 있었다).
- 2026-09-05 · **미리보기는 board 파트를 기본으로 하고, 파트 탭으로 다른
  파트(부속·조립물)도 전환해 볼 수 있게 했다.** "할 일"엔 미리보기 하나만
  적혀 있었지만, 팀 이름이 점수 기록칸에도 나오는 배치(`IDE-003`)가 이미
  있어 탭 전환이 자연스러웠다. 부속·조립물 파트는 슬롯이 없으면 배경만
  보인다.
- 2026-09-05 · **마커 드래그(자유 좌표 편집)는 이번에도 범위 밖으로 남겼다.**
  프리셋 적용(대형 선택)은 review 이후 사용자 요청으로 추가했지만, 마커를
  손으로 옮기는 조작까지는 "할 일"에 없다 — 옮기고 싶으면 다른 대형을
  고르는 방식으로만 가능하다. 드래그 배치가 필요해지면 별도 이슈로 뺀다.
- 2026-09-05 · **대형 선택을 그룹 단위 버튼으로 붙였다.** `game.presets`를
  `groupId`로 필터링해 그룹 섹션(이름·색 슬롯 아래, 등번호 격자 위)에
  `FormationPicker`를 넣었다 — 버튼 라벨은 `formationId`(예: `4-4-2`)를
  쓰고, `IDE-003`이 이미 제공하는 `applyPreset`으로 그 그룹 마커의 좌표만
  바꾼다. 어느 프리셋을 눌렀는지는 별도 상태(`selectedPresetByGroup`)로만
  표시하고 좌표와는 분리했다 — 좌표는 이미 `customization.positions`가
  진실의 원본이라 프리셋 선택 여부를 좌표에서 역산할 필요가 없었다.
  실제 브라우저에서 홈 팀만 3-5-2로 바꾸고 원정 팀은 4-4-2로 그대로
  남는 것을 확인해 그룹별 독립 적용을 검증했다.
- 2026-09-05 · **로컬 저장·하이드레이션에 `useSyncExternalStore` 패턴을 썼다.**
  `useEffect` 안에서 곧장 `setState`를 부르면 프로젝트 lint
  (`react-hooks/set-state-in-effect`)가 막는다 — Next.js 16의
  `eslint-config-next` 기본값이 React Compiler 규칙 세트를 포함한다. 대신
  `useHydrated`로 하이드레이션 여부를 판단해 `EditorForm`의 `key`를 바꿔
  마운트 시점의 초기값(저장값 또는 기본값)을 정하는 방식으로 우회했다.
- 2026-09-05 · 실제 브라우저(playwright, headless chromium)로 `/games/soccer/edit`를
  띄워 등번호·팀명·팀색 변경과 미리보기 반영, 파트 전환, 슬롯 클릭 → 입력
  포커스 이동, 되돌리기, 대형 선택(그룹별 독립 적용)을 모두 확인했다.
  `npm run typecheck`·`lint`·`test:run`(134개)·`build` 전부 통과한다.
- 2026-09-05 · **`choice` 슬롯 입력을 shadcn/ui로 교체하며 `npx shadcn@latest
  init`을 처음 돌렸다.** CLI 기본값(`base-nova` 프리셋, `@base-ui/react` 기반)이
  `components.json`·`src/components/ui/button.tsx`·`src/lib/utils.ts`를
  만들고 `src/app/globals.css`를 다시 썼다 — `select`는 이어서
  `npx shadcn add select`로 추가했다.
- 2026-09-05 · **init이 건드린 `globals.css`를 그대로 두지 않고 다크모드
  방식을 되돌렸다.** CLI 기본값은 다크모드를 `.dark` 클래스로 켜는 방식
  (`@custom-variant dark (&:is(.dark *))`)인데, 이 사이트는 처음부터 시스템
  설정(`prefers-color-scheme`)에 자동으로 반응하는 방식이었고 `.dark` 클래스를
  붙이는 코드가 어디에도 없다 — 그대로 뒀으면 다크 모드가 조용히 완전히
  꺼졌을 것이다. `@custom-variant` 선언을 지워 Tailwind v4 기본값(미디어
  쿼리 기반 `dark:`)으로 돌리고, `.dark { … }` 블록의 색 토큰을
  `@media (prefers-color-scheme: dark) { :root { … } }`로 옮겼다. 자기
  자신을 참조하던 `--font-sans: var(--font-sans)`(init이 만든 순환 참조)도
  `var(--font-geist-sans)`로 고쳤다. 실제 브라우저에서 `colorScheme: 'dark'`
  컨텍스트로 열어 별도 토글 없이 시스템 설정만으로 어두운 배경·밝은 글자가
  나오는 것을 확인했다.
  - 안 쓰는 `sidebar-*`·`chart-*`·`card` 토큰은 `globals.css`에서 뺐다 —
    이 사이트에 사이드바·차트·카드 컴포넌트가 없다. 필요해지면 그 컴포넌트를
    추가할 때 shadcn CLI가 다시 채운다.
- 2026-09-05 · **`SlotField`의 `choice` 케이스만 `<Select>`(shadcn/ui)로
  바꿨다.** `<label htmlFor>`는 `SelectTrigger`의 `id`로 그대로 연결해
  접근성 연결(라벨 클릭 → 포커스)을 유지했다. 팝업이 포털로 렌더링돼
  `fireEvent.click`만으로는 항목 선택이 반영되지 않아 테스트에
  `@testing-library/user-event`를 새로 추가했다.
- 2026-09-05 · 실제 브라우저로 선택 열기·항목 고르기·값 반영과 다크모드
  자동 반응을 확인했다(스크린샷). `npm run typecheck`·`lint`·`test:run`
  (135개)·`build` 전부 통과한다.
- 2026-09-05 · `BoardPreview` 전용 테스트를 새로 추가해(`fetch`를 스텁해
  실제 `public/games/soccer/*.svg`를 읽어 들이는 방식) 원형 아트워크가
  팀 색으로 칠해지는 것, 일러스트로 바꾸면 그 아트워크가 반영되는 것,
  아직 못 불러왔을 때 대체 표시로 시작하는 것 세 가지를 검증했다. 실제
  브라우저로도 확대 스크린샷을 찍어 사람 모양과 등번호가 함께 보이는 것을
  확인했다. `npm run typecheck`·`lint`·`test:run`(140개)·`build` 전부
  통과한다.
- 2026-09-05 · `review` → `done`. 수용 기준 4개 모두 충족되고, 위 재검증에서
  typecheck·lint·test·build가 전부 통과했다. PR로 머지한다.
