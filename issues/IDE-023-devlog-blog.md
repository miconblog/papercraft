# IDE-023 · 제작일지 — 관리자 화면에서 쓰고 게시하는 글

| 항목        | 값                    |
| ----------- | --------------------- |
| 상태        | `todo`                |
| 영역        | frontend              |
| 우선순위    | P2                    |
| 마일스톤    | M3                    |
| 추정        | 4d                    |
| 추천 모델   | Opus 5                |
| 추천 Effort | high                  |
| 선행        | `IDE-013` · `IDE-022` |
| 후행        | —                     |

## 배경

사용자 요청(2026-09-09). **게임을 만들며 겪은 것을 제작일지로 남겨 사이트에
게시하고 싶다.** 지금 이 사이트에는 글을 담을 자리가 없다 — 게임 카탈로그와
에디터, 인쇄뿐이다.

이 사이트의 게임은 전부 "옛 인쇄본을 다시 그린 것"이라 만드는 과정에 이야기가
있다(`IDE-015`의 로빈슨 도법 지도, `IDE-018`의 무게중심 잡기, `IDE-021`의 눈·코를
어떻게 뽑았나). 그 이야기가 사람을 데려오는 유일한 밑천이기도 하다 — `IDE-013`을
붙여 놓고 보니 홍보가 없으면 유입도 없다.

## 글은 관리자 화면에서 쓴다

**2026-09-09 사용자 결정.** 저장소에 마크다운 파일을 커밋하는 쪽이 만들 것이
훨씬 적지만, 컴퓨터 앞에서만 글을 쓸 수 있다. 브라우저에서 쓰면 폰으로도
쓰고 고칠 수 있다.

값은 `IDE-013`이 세운 `daddys_craft` 스키마에 둔다. `IDE-022`가 게임 오픈일로
같은 일을 하므로, **표를 만들고 여는 규약도 시각 판정도 그 이슈와 나눠 쓴다** —
예약 게시는 예약 공개와 같은 문제다.

## 본문을 어떤 형식으로 담나

마크다운이 자연스럽다 — 이 저장소의 문서가 전부 마크다운이고 사용자가 이미
그 문법에 익숙하다.

**그런데 마크다운을 HTML 로 그리는 순간 XSS 가 생긴다.** 글을 쓰는 사람이
관리자 하나뿐이라 "누가 나쁜 것을 넣겠나" 싶지만, 관리자 세션은 비밀번호
하나로 지켜지고(`IDE-013`) 그게 새면 사이트 전체에 스크립트를 심을 수 있는
길이 된다. 그리는 쪽에서 막아야 한다.

정할 것: 마크다운 라이브러리와 정화(sanitize) 방식, 그리고 **저장할 때 한 번
HTML 로 바꿔 두느냐 읽을 때마다 그리느냐**. 뒤쪽이 고치기 쉽고 앞쪽이 빠르다.

## 사진을 어디에 두나

제작일지에는 사진이 들어간다 — 종이로 뽑아 접은 것, 아이가 놀고 있는 것.
후보는 Supabase Storage 와 저장소 안 `public/`이다. **미정이다.** Storage 는
브라우저에서 올릴 수 있어 "폰으로도 쓴다"와 맞지만 버킷 정책을 새로 세워야
하고, `public/`은 아무것도 안 세워도 되지만 다시 컴퓨터 앞으로 돌아간다.

아이 사진이 올라갈 수 있다는 점도 함께 본다 — 공개 버킷에 그대로 두는 것이
맞는지, 얼굴이 나온 사진을 올릴 생각인지 사용자에게 확인이 필요하다.

## 공개 쪽 화면

- 목록과 글 하나 — 경로는 정해야 한다(`/log` · `/diary` · `/making`)
- 홈에서 최근 글로 들어가는 길. 홈은 지금 히어로 → 아이와 함께 → 만드는 순서 →
  게임 목록이다. **글이 게임을 밀어내지 않는 자리**를 찾는다
- `sitemap`·OG 메타데이터. 글은 검색으로 들어오라고 쓰는 것이라 이게 빠지면
  쓴 보람이 없다
- 방문 통계는 저절로 잡힌다 — `PageViews`가 루트 레이아웃에 있어 새 경로도 센다

## 할 일

- [ ] 마이그레이션 — 글 표(제목 · 슬러그 · 본문 · 공개 시각 · 작성·수정 시각).
      `IDE-013` 규약(RLS 켜고 정책 없음 · 권한 회수 후 `service_role`만)을 지킨다
- [ ] 관리자 글 목록·작성·수정 — `/admin` 아래에 둔다(통계에서 빠진다)
- [ ] 마크다운 그리기와 정화 — 라이브러리·방식을 정하고 결정 기록에 남긴다
- [ ] 사진 올리기 — 두는 자리를 먼저 정한다(**사용자 확인 필요**)
- [ ] 공개 목록·글 화면과 경로 확정
- [ ] 예약 게시 — `IDE-022`의 시각 판정을 나눠 쓴다. 오픈 전 글은 목록에도 URL
      로도 안 열리고 관리자 세션만 미리 본다
- [ ] `sitemap`·OG 메타데이터에 글 싣기
- [ ] 홈에서 최근 글로 가는 길
- [ ] 테스트 — 정화(스크립트가 담긴 본문) · 예약 게시 경계 · 슬러그 중복 ·
      Supabase 없을 때 사이트가 그대로 뜨는지

## 수용 기준

- [ ] 관리자 화면에서 글을 쓰고 게시하면 공개 목록에 나온다
- [ ] 폰 브라우저에서도 쓰고 고칠 수 있다
- [ ] 본문에 `<script>`나 `onerror` 를 넣어도 실행되지 않는다
- [ ] 게시 시각을 미래로 두면 목록에도 URL 로도 안 열리고, 시각이 지나면 사람
      손 없이 열린다
- [ ] 글 페이지에 OG 메타데이터가 있고 `sitemap`에 실린다
- [ ] 글 방문이 `/admin` 과 달리 통계에 잡힌다
- [ ] Supabase 를 꺼도 사이트가 그대로 뜬다 — 글 자리만 비고 게임은 멀쩡하다

## 결정 기록

- **2026-09-09 — 글은 관리자 화면에서 쓴다**(사용자 결정). 저장소에 마크다운을
  커밋하는 쪽이 만들 것은 적지만 컴퓨터 앞에서만 쓸 수 있다.
- **2026-09-09 — 예약 게시는 `IDE-022`와 같은 문제로 본다.** 시각 판정과 정적
  생성의 관계를 두 번 풀지 않는다. 그래서 `IDE-022`가 선행이다.

## 물어볼 것

- 사진을 어디에 둘까 — Supabase Storage 인가 저장소 `public/` 인가
- 아이 얼굴이 나온 사진을 올릴 생각인가. 공개 버킷 그대로 두어도 되는가
- 공개 경로 이름 — `/log` · `/diary` · `/making` 가운데 무엇인가
